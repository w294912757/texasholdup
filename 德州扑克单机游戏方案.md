# 单机德州扑克游戏方案

> 文档状态：1.0.0 开发完成 / 1.1.0 阶段 E 开发中
> 技术约束：Vue 3、TypeScript、Element Plus、pnpm、Sass  
> 当前阶段：1.1.0 阶段 E（F-014 无障碍增强）开发完成，暂停等待 V-024 人工验证

## 1. 产品定位

一款界面简单直接、以深色视觉为主的浏览器端单机德州扑克游戏。玩家使用无密码本地账号与 AI 对手进行标准无限注德州扑克对局，通过获胜积累经验、提升等级，并在完整记录与逐行动回放中复盘决策。

推荐首版定位为：

- 6 人桌，1 名真人玩家加 1 至 5 名 AI，默认满桌。
- 标准 52 张牌、无限注德州扑克、现金桌式筹码结算。
- 无后端、无联网依赖；使用无密码本地账号隔离数据，首次启动自动创建并进入 `admin` 账号。
- 深色界面、低装饰、牌桌与操作优先；不制作营销式首页、超大标题或层层嵌套的卡片。
- 完整适配常规桌面、网页小窗口和平板/手机窄屏，小窗口下仍能看清牌面、筹码并完成全部下注操作。
- 只使用虚拟筹码，不涉及充值、提现、兑换、广告或任何真钱赌博功能。

### 1.1 首版不做

- 联机匹配、好友房、观战和排行榜。
- 真实货币、内购和支付。
- 多桌同时进行。
- 自定义规则、短牌、奥马哈等衍生玩法。
- 云同步和跨设备存档。
- AI 语音、聊天或生成式对话。
- 密码、权限控制、远程身份认证和多设备账号同步。

## 2. 已确认的硬性要求

### 2.1 前端工程

- 使用 Vue 3、Composition API、TypeScript、Element Plus、pnpm、Sass。
- 所有可见元素、布局容器和有交互意义的元素都必须有符合语义的类名。
- 禁止 `style` 属性、模板内样式绑定以及脚本直接写入元素样式。
- 样式统一放在 Sass 文件或 Vue 单文件组件的 `style` 区域，通过状态类和 CSS 自定义属性控制变化。
- 类名建议采用 BEM 风格，例如“牌桌、牌桌的公共牌区、牌桌的结算状态”，避免 `box1`、`left-wrap`、`red-text` 等只描述位置或外观的命名。
- Element Plus 组件必须通过业务语义类包裹或附加类名，不依赖自动生成的内部类作为业务选择器。

### 2.2 对局记录

记录粒度必须达到：牌局 -> 回合阶段 -> 玩家行动。

每手牌至少记录：

- 牌局编号、开始和结束时间、规则版本、随机种子标识、按钮位、盲注、座位信息。
- 每位玩家的起始筹码、座位、最终筹码和净输赢；真人记录账号等级，AI 的真实难度只保存在内部回放数据中，不进入玩家可见字段。
- 洗牌后牌序或足以重放的确定性数据；不得在牌局进行时向界面或 AI 暴露未公开牌。
- 底牌发放、公共牌发放、烧牌行为和牌面。
- 翻牌前、翻牌、转牌、河牌四个阶段的开始状态和结束状态。
- 每次玩家行动：行动序号、玩家、可选动作、实际动作、投入前后筹码、跟注额、下注或加注至多少、底池变化、行动时间。
- 过牌、跟注、下注、加注、弃牌、全下、超时等动作；非法动作不得写入正式记录。
- 主池、边池的形成过程、参与者、金额和最终归属。
- 摊牌顺序、可见底牌、最佳五张牌、牌型、胜负原因、平分底池和余数筹码分配。
- 牌局结束原因，例如所有对手弃牌、摊牌获胜、平局、玩家离桌或异常中止。
- 本手或所属会话关联的经验结算事件、计算明细以及升级前后状态。
- 每手结算后的 AI 阵容变化，包括离场 AI、空缺座位、补位 AI、入离场顺序和触发时的公开状态；AI 离场评分、真实难度和策略参数仅保存在内部数据中。

记录功能应支持：

- 按日期、输赢、底池大小、牌局编号和是否中途离桌筛选；不提供 AI 难度筛选。
- 查看牌局摘要与逐行动时间线。
- 上一步、下一步、按阶段跳转、自动播放、调速和暂停。
- 在每个行动节点恢复当时的座位筹码、底池、公共牌、当前行动者和合法动作。
- 展示“当时可知信息”和“全局复盘信息”两种视图；全局信息只能在该手牌结束后查看。
- 导出、导入 JSON 记录，并进行版本与完整性校验；玩家可见的对局导出必须移除 AI 难度和策略参数。
- 删除单条记录、批量删除和清空记录，删除前二次确认。

### 2.3 等级与经验

- 获胜后获得经验值，经验达到阈值时自动升级。
- 玩家等级不直接指定或锁定 AI 等级，只影响不同难度 AI 被匹配到的概率。
- 候选范围未触及全局边界时，玩家应有机会遇到较弱、相近和较强 AI；最低等级保留少量相邻偏强对手，最高等级保留相对较弱对手，避免整桌强度单一。
- 玩家可以在个人档案中手动降到任意已解锁等级。
- 玩家不能通过界面手动升级，也不能直接选择 AI 难度。
- 手动降级不删除终身累计经验和历史最高等级，但清空当前等级内的升级进度，避免降级后立即自动升回。
- 降级后也不提供“一键恢复已解锁等级”；只能继续获胜、重新积累当前等级经验，由系统自动升级。
- 每次等级、经验变化都记录原因、时间、关联牌局和变化前后值。
- 单机存档只能保证产品界面内的升级规则，无法承诺抵御用户直接修改浏览器数据；首版不做伪安全的本地防作弊。

建议将“获胜”定义为一次完整牌桌会话结束时净盈利，而不是赢下任意一个小底池。这样可以避免通过反复开局、偷盲刷经验。经验只根据净盈利、有效手数和连续重复对局衰减计算，并设置单场上限；不使用 AI 难度作为经验参数，避免通过经验反推出对手强弱。

### 2.4 匹配与 AI 难度隐藏

- 每次开始牌局或离桌重匹配时，由匹配器独立生成一桌 AI，不允许玩家选择或查看 AI 难度。
- 匹配以玩家当前生效等级为基准，将候选 AI 分为“较弱、相近、较强”三个内部区间，再按权重为每个座位抽样。
- 首轮实玩发现完整 1–12 档候选池会让初始等级过早遇到顶级 AI，因此改用随等级逐步上移的有限窗口。校准后的范围和单席基础抽取比例如下：

| 玩家等级 | 可匹配档位 | 较弱 AI      | 相近 AI       | 较强 AI    |
| -------- | ---------- | ------------ | ------------- | ---------- |
| Lv.1     | 1–3 档     | 无           | 1–2 档，85%   | 3 档，15%  |
| Lv.2     | 1–4 档     | 无           | 1–3 档，80%   | 4 档，20%  |
| Lv.3     | 1–5 档     | 1 档，10%    | 2–4 档，70%   | 5 档，20%  |
| Lv.4     | 1–6 档     | 1–2 档，15%  | 3–5 档，65%   | 6 档，20%  |
| Lv.5     | 1–7 档     | 1–3 档，15%  | 4–6 档，65%   | 7 档，20%  |
| Lv.6     | 2–8 档     | 2–4 档，20%  | 5–7 档，60%   | 8 档，20%  |
| Lv.7     | 3–9 档     | 3–5 档，20%  | 6–8 档，60%   | 9 档，20%  |
| Lv.8     | 4–10 档    | 4–6 档，25%  | 7–9 档，60%   | 10 档，15% |
| Lv.9     | 5–11 档    | 5–7 档，25%  | 8–10 档，60%  | 11 档，15% |
| Lv.10    | 6–12 档    | 6–8 档，30%  | 9–11 档，55%  | 12 档，15% |
| Lv.11    | 7–12 档    | 7–9 档，35%  | 10–12 档，65% | 无         |
| Lv.12+   | 8–12 档    | 8–10 档，35% | 11–12 档，65% | 无         |

- 候选难度达到条件时，每桌至少包含两个难度区间，禁止生成整桌同一难度的 AI。
- 在最低或最高难度边界，不存在的较弱或较强区间概率为零，不得越过本级候选窗口抽取 AI。
- 手动降级改变当前生效等级，因此会改变下一次匹配概率；历史最高等级只用于判断玩家曾经解锁过哪些等级。
- AI 的名称、头像、下注延迟和台词不得与难度固定绑定，避免形成可直接识别的难度标签。
- 牌桌、结算、记录、回放、统计和公开导出都不展示 AI 等级、难度区间、策略版本或匹配权重。
- 内部仍需保存实际 AI 配置、匹配规则版本和随机种子，以支持确定性回放、问题定位和版本迁移。
- 本地应用无法阻止技术用户检查浏览器数据库或源码；“隐藏”指产品界面和普通导出不主动泄露，不承诺安全对抗。

#### 2.4.1 每手结算后的 AI 轮换与补位

- 仅在一手牌完成底池结算并持久化后、下一手收取盲注前评估 AI 阵容，手牌进行中不得换人。
- 每名在桌 AI 独立计算离场结果。筹码归零的 AI 强制离场；其他 AI 使用剩余筹码占入桌筹码的比例、最近 5 手净输赢、连胜或连败、连续在桌手数和低概率基础离场值综合计算，因此每手允许没有 AI 离场，也允许多名 AI 离场。
- 新入桌不足 3 手的 AI 除筹码归零外不参与自愿离场；为避免阵容频繁完全重置，自愿离场人数默认每手最多 2 名，强制离场不受此上限约束。窗口长度、各项权重、基础概率和人数上限集中配置并带规则版本。
- 每个离场空位立即补入一名新 AI，保持玩家开桌时选择的 AI 数量不变。离场 AI 携带其剩余筹码退出，新 AI 按本桌配置的标准买入入场；两者均作为桌面筹码流入/流出记录，不改变真人账号余额。
- 补位使用玩家当前生效等级对应的候选难度范围：先沿用 2.4 节权重抽取“较弱、相近、较强”内部区间，再在命中的区间内随机选择具体 AI 配置。候选范围必须保留可用的强弱跨度，不得因玩家等级把补位锁定为单一难度。
- 新 AI 不得与当前在桌 AI 使用相同身份，并应避免刚离场的同一身份立即返回；名称、头像、入场提示和下注延迟仍不得泄露难度。
- 补位在按钮移动和下一手盲注确定前完成，新 AI 从下一手开始正常参与按钮位和盲注轮转，不继承离场 AI 的近期表现、决策种子或未完成状态。
- 离场判定与补位抽样使用牌桌确定性随机源。离场事件、判定输入摘要、补位事件、实际内部配置和随机状态必须在同一事务中保存；刷新、关闭或恢复后不得重新判定或重新抽取。
- 玩家可见界面和回放只展示“AI 离场”和“AI 入座”及座位变化，不显示离场评分、离场原因中的策略数据、候选难度区间或真实难度。

### 2.5 离桌与重新匹配

- 玩家可在牌局任意时刻选择“离桌并重新匹配”，无需等待当前手牌或会话结束。
- 当前手牌进行中时，离桌被定义为不可撤销的强制弃权：该手已投入主池或边池的全部筹码保留在底池中，玩家失去争夺这些底池的资格。
- 即使玩家已经全下，离桌仍表示主动放弃底池资格；确认弹窗必须明确展示将损失的已投入筹码。
- 尚未投入底池的桌面筹码退回当前账号的虚拟筹码余额，不因离桌额外扣除。
- 若处于两手牌之间，当前没有已下注筹码，则可无损离桌。
- 原牌桌由 AI 快速完成当前手牌并写入完整结算记录；玩家无需等待即可进入新匹配。
- 中途离桌的会话不发放胜利经验，已完成手牌和已产生的等级流水不回滚，并计入离桌次数统计。
- 重新匹配必须重新运行概率匹配器，不复用上一桌 AI 组合。
- 离桌命令、损失金额、未投入筹码返还、旧桌结算和新桌创建都必须事务化，刷新或重复点击不得重复扣款。

为使“损失当前已下注筹码”成为真实代价，方案采用账号级虚拟筹码余额：入桌买入从账号余额扣除，离桌或正常结束后将剩余桌面筹码退回账号。具体初始余额、最低买入和破产补充规则在编码前冻结。

### 2.6 本地账号

- 首次启动自动创建名为 `admin` 的默认账号并直接进入，不要求输入密码。
- 支持创建、切换、重命名和删除本地账号；账号显示名忽略大小写后不得重复。
- 每个账号使用不可变内部 ID，显示名修改不得改变其数据归属。
- 等级、经验、历史最高等级、虚拟筹码、牌局记录、回放、统计、收藏、备注、等级流水和未完成状态全部按账号隔离。
- 切换账号不会结束当前账号牌局；每个账号分别保存唯一活动牌局，切回账号后从最后一次已提交状态继续。
- 当前账号不允许删除；删除其他账号会删除其全部数据，必须进行二次确认；系统始终至少保留一个账号。
- 支持导出当前账号、导入为新账号以及导出全部账号备份，导入不得静默覆盖同名或同 ID 数据。
- 无密码账号只是同一浏览器内的数据分区，不提供隐私隔离、身份验证或防止他人访问的能力，界面必须如实说明。

### 2.7 自动保存与现场恢复

- 自动保存采用“先持久化、后推进”原则：每次真人或 AI 的有效决策必须与新状态、事件和筹码变化在同一个 IndexedDB 事务中提交成功，之后才能轮到下一位玩家。
- 除玩家决策外，发牌、阶段切换、底池创建、摊牌、结算、经验变化、离桌和重新匹配等所有状态变化也必须立即保存。
- 每手结算后的 AI 离场判定、席位移除、补位抽样和新 AI 入座作为一个不可分割的阵容更新事务保存，成功后才能开始下一手。
- 活跃牌桌快照至少包含账号、会话、手牌阶段、行动序号、座位、筹码、投入额、主池/边池、底牌、公共牌、剩余牌堆、按钮位、当前行动者、最小加注和确定性随机状态。
- 退出游戏、刷新、浏览器崩溃或关闭浏览器只会暂停牌局；再次进入同一账号时直接恢复到最后一个已提交行动后的现场，不视为离桌，不扣除额外筹码。
- 已提交行动不可通过刷新或重新打开浏览器撤销；恢复时不得重新洗牌、重新抽取 AI、重新计算已提交 AI 行动或退回已投入筹码，避免反复读档获利。
- 若 AI 决策计算中途关闭浏览器且尚未提交，恢复后使用相同公开状态、AI 版本和决策种子重新计算，必须得到相同结果。
- 每个命令携带会话 ID、预期行动序号和幂等键；重复提交、旧页面提交或恢复重试不得生成第二条行动或重复结算。
- 不依赖 `beforeunload` 完成关键保存；该事件只能用于提示，正确性由每次状态事务保证。
- 同一账号同一时间只允许一个标签页控制活跃牌桌；其他标签页进入只读或接管确认状态，防止并发产生两条时间线。
- 首页发现活跃牌桌时优先显示“继续牌局”，不得通过“开始匹配”覆盖旧现场；如需放弃，必须走明确的离桌结算流程。

### 2.8 列表牌局与实时 GTO 参考

- 游戏主区域不模拟真实椭圆牌桌，改为公共牌概览与高密度玩家列表，集中展示座位、筹码、手牌、下注和行动状态。
- 玩家列表必须支持独立滚动，操作区固定可见；桌面、小窗口和手机竖屏均不得出现横向溢出或关键操作遮挡。
- 游戏过程中提供实时“GTO 参考”入口；桌面端悬浮显示主要建议，点击后在二级弹窗中查看完整混合频率，触屏设备使用点击弹窗。
- 本地参考只使用用户底牌、公共牌、未弃牌人数、底池、有效筹码和合法动作，不读取 AI 隐藏底牌或难度参数。
- 首版采用未知牌蒙特卡洛权益、底池赔率和 SPR 生成近似混合策略，界面明确说明不等同于完整 GTO 求解器结果。

## 3. 建议补充的产品要求

### 3.1 规则必须先冻结

- 明确采用标准无限注德州扑克。
- 明确小盲、大盲、最小加注、全下不足额加注、行动重开条件。
- 支持主池和多个边池，边池只能由实际投入该池的玩家争夺。
- 多名获胜者平分底池；无法整除的余数筹码按按钮位之后顺时针优先分配。
- 使用最佳五张牌判定，正确处理公共牌成牌、踢脚、同花比较和完全平局。
- 玩家主动离桌按 2.5 节执行；筹码归零、AI 淘汰、会话结束、重新买入和破产补充规则必须明确。
- 建议首版不收抽水，避免影响结算和经验公平性。

### 3.2 AI 公平性与内部梯度

- AI 只能读取真人玩家在同一时点能读取的公开信息，以及自身底牌。
- AI 禁止访问牌堆顺序、其他玩家底牌、未来公共牌或界面层保存的隐藏状态。
- 难度提升应来自更好的手牌强度估算、位置意识、范围推断、底池赔率、下注尺度、诈唬平衡和历史行为适应。
- 难度参数和匹配权重必须可配置、可测试、可版本化，不把等级或匹配判断散落在组件中。
- AI 决策应有最短和最长响应时间，并允许玩家关闭等待动画，但响应时间不得改变决策结果。
- 同一局面、同一 AI 版本和同一决策种子应能复现相同结果，便于定位问题和回放。
- 每次 AI 决策在内部记录算法版本和决策参数摘要，但不在玩家界面、普通对局导出或复盘中展示。

推荐内部难度梯度，仅供引擎和测试使用：

| 等级段 | 对手能力变化                                       |
| ------ | -------------------------------------------------- |
| 1-3    | 基于自身牌力与简单阈值行动，下注尺度较固定         |
| 4-6    | 加入位置、公共牌结构、底池赔率和基础诈唬           |
| 7-9    | 加入玩家范围、组合数、混合策略和多街规划           |
| 10+    | 加入对玩家长期倾向的有限适应，但设置遗忘与强度上限 |

### 3.3 存档与数据安全

- 使用 IndexedDB 保存账号、档案、虚拟筹码、设置、牌局记录和回放事件，不使用 `localStorage` 承载大量记录。
- 所有账号私有对象必须携带 `accountId` 并使用账号作用域索引，任何查询都不得依赖当前界面筛选来实现隔离。
- 全局设置仅限不涉及玩家进度的设备偏好；等级、历史、统计和游戏设置不得跨账号共享。
- 所有持久化对象包含数据模式版本，升级应用时提供迁移策略。
- 自动保存应发生在每个有效行动后，异常刷新后可选择恢复未完成牌局或将其标记为中止。
- 提供当前账号和全部账号两种备份范围，导入前自动备份并明确展示新增、冲突和覆盖内容。
- 记录容量达到阈值时先提示，不静默删除历史数据。
- 提供“恢复当前账号默认设置”“清除当前账号”和“清除全部本地数据”，三者必须明确区分。
- 随机数来源、种子生命周期和重放方式必须单独测试；不得使用可被 AI 预测并利用的共享可变随机状态。

### 3.4 可用性与无障碍

- 整体采用低对比眩光的深色主题：中性深灰作为应用背景，克制的绿色作为牌桌区域，红/蓝等颜色只承担明确状态和操作含义。
- 第一屏直接呈现当前账号、等级、筹码和“开始匹配/继续牌局”，不设置宣传页、欢迎大图或功能介绍卡片。
- 牌桌始终是页面视觉主体，弱化边框、阴影和装饰；同一信息只出现一次，非核心信息进入抽屉或弹层。
- 所有核心操作都可用鼠标和键盘完成。
- 弃牌、跟注、下注/加注按钮在位置、颜色和文案上保持稳定，避免误操作。
- 下注使用滑块、快捷比例和数值输入三种方式；数值输入必须校验最小值、最大值和全下边界。
- 离桌、删除账号和清空数据必须确认；下注动作不提供撤销，使用稳定按钮位置和可选确认降低误触。
- 不只依赖颜色表达花色、输赢、轮到行动或禁用状态。
- 支持减少动态效果、音效开关、背景音乐开关和音量分别调节。
- 发牌与筹码动画不得阻塞规则引擎，刷新动画不应改变结果。
- 牌面、筹码、底池、行动状态在常见缩放比例和网页小窗口下清晰可读且不重叠。
- 核心牌局区域不得依赖横向滚动才能完成操作；页面发生纵向滚动后，行动区仍保持可见或可立即到达。

响应式布局按容器可用空间切换，不对整个牌桌做等比缩小：

| 布局档位 | 参考尺寸                           | 牌桌调整                                                               |
| -------- | ---------------------------------- | ---------------------------------------------------------------------- |
| 标准桌面 | 1280×720 及以上                    | 完整环形座位、常驻行动简表和完整下注控件                               |
| 紧凑窗口 | 宽度至少 640，且未达到标准桌面条件 | 缩短玩家信息、收起行动简表、压缩桌边留白，操作区固定在底部             |
| 窄屏     | 宽度 360 至 639，高度至少 520      | 对手改为紧凑座位带，公共牌与底池居中，次要信息使用抽屉，下注控件分两行 |
| 极矮窗口 | 任意宽度且高度低于 520，优先级最高 | 隐藏非核心装饰和历史摘要，保留公共牌、当前行动、筹码与全部决策按钮     |

- 断点应以牌桌容器尺寸为准，并同时考虑宽度与高度，避免只按设备名称判断。
- 字号不随视口宽度连续缩放；用稳定字号、最小宽高、网格轨道和内容折行保证可读性。
- 在 1920×1080、1366×768、1024×768、640×520 和 360×640 下进行自动截图与交互验收。

### 3.5 统计与复盘

- 提供总手数、胜率、净输赢、摊牌率、摊牌胜率、主动入池率、翻牌前加注率等基础指标。
- 统计口径、样本范围和计算版本必须固定，避免升级后同一指标含义变化。
- 可按日期、位置、起手牌、玩家当时等级和是否中途离桌筛选统计，不按 AI 难度筛选。
- 标记收藏牌局并添加本地备注。
- 不在首版给出“最优打法”或胜率承诺；复盘先忠实还原事实，再考虑策略建议。

### 3.6 游戏设置

- 可调动画速度、AI 思考时间、音量、牌面样式和回放默认速度。
- 对局开始前可选择 AI 数量、入桌买入、盲注结构和新手提示；AI 难度完全由隐藏匹配器决定。
- 对局中不得修改影响公平性的规则参数；修改后仅对下一场生效。
- 提供新手提示开关，提示只解释规则与控件，不代替玩家决策。

## 4. 核心页面与流程

### 4.1 首页 / 档案概览

- 顶部提供当前账号和账号切换入口，不额外设置登录页。
- 展示当前等级、当前经验、距下一级经验、历史最高等级和虚拟筹码余额。
- 继续未完成牌局、开始匹配、对局记录、统计、账号管理和设置入口。
- 手动降级入口及影响说明。

### 4.2 匹配与入桌配置

- 选择 AI 数量、入桌买入、盲注和可选的新手提示，不出现任何 AI 难度选项。
- 点击开始后先锁定买入，再由匹配器按当前玩家等级生成隐藏的 AI 组合。
- 匹配结果只展示 AI 名称、头像和座位，不展示难度、等级或“强弱”描述。
- 展示规则和买入摘要，确认后锁定本场配置。

### 4.3 牌桌

- 中央公共牌区、主池/边池、按钮位和当前阶段。
- 牌张、点数和花色必须在桌面、小窗口和窄屏中保持清晰可辨；牌张按断点使用稳定尺寸，放大后不得挤出牌桌、遮挡座位信息或压缩操作区。
- 环形座位区展示玩家名称、筹码、当前投入、状态与最近动作。
- 固定操作区提供弃牌、过牌/跟注、下注/加注、全下及下注控制。
- 辅助区提供本手行动简表、设置、音效和“离桌并重新匹配”。
- 离桌确认展示当前已投入且不会退回的筹码、可退回余额，以及本场不会获得胜利经验的提示。
- 结算层展示获胜者、牌型、底池分配、净输赢与经验变化。

### 4.4 对局记录

- 左侧或顶部筛选，主体为可排序的牌局列表。
- 单局详情包含摘要、阶段分组的行动时间线、底池明细和经验明细。
- 从详情进入全桌回放，回放布局复用牌桌的只读呈现组件。

### 4.5 统计与设置

- 统计页展示关键指标、趋势和筛选条件。
- 设置页管理显示、声音、动画、当前账号数据导入导出和清理。

### 4.6 账号管理

- 列出本地账号、当前账号、创建时间和最近使用时间，不展示其他账号的等级、筹码或对局摘要。
- 提供创建、切换、重命名、导出和删除操作。
- 账号切换优先从页面顶部完成；账号管理页用于低频维护操作。
- 涉及进行中牌局的切换统一进入离桌确认流程，不提供绕过入口。

## 5. 推荐技术方案

### 5.1 工程选型

| 领域       | 推荐方案                      | 说明                                                          |
| ---------- | ----------------------------- | ------------------------------------------------------------- |
| 构建       | Vite + Vue 3 + TypeScript     | 轻量、类型支持完整                                            |
| 包管理     | pnpm                          | 使用锁文件固定依赖版本                                        |
| UI         | Element Plus                  | 用于表单、弹窗、表格、筛选等工具型界面                        |
| 状态       | Pinia                         | 分离账号、牌局、匹配、档案、设置和记录状态                    |
| 路由       | Vue Router                    | 页面级导航与回放深链                                          |
| 样式       | Sass + BEM + CSS 自定义属性   | 禁止内联样式，主题参数集中管理                                |
| 本地数据库 | IndexedDB 封装库              | 支持事务、索引、查询和模式迁移                                |
| 规则校验   | 成熟的扑克牌型评估库          | 牌型核心不自行凭感觉实现，候选库需先验证 TS、许可证和边界用例 |
| 单元测试   | Vitest                        | 规则、结算、经验、AI 合法动作测试                             |
| 组件测试   | Vue Test Utils                | 操作面板、记录详情、状态展示测试                              |
| 端到端测试 | Playwright                    | 完整牌局、刷新恢复、回放、升级/降级流程                       |
| 代码质量   | ESLint + Stylelint + Prettier | 将类名、禁用内联样式等约束纳入检查                            |

### 5.2 分层建议

- `domain`：纯 TypeScript 领域模型，包括账号、筹码账本、牌、牌堆、座位、下注轮、底池、结算、经验和等级规则。
- `engine`：牌局状态机、合法动作生成、事件处理和确定性随机数。
- `ai`：只接收脱敏后的公开牌局视图和自身底牌，返回合法动作意图。
- `matching`：根据玩家当前等级、内部权重和组合约束生成初始 AI 阵容，并在每手结算后完成离场判定与空位补充，不向表现层返回难度字段。
- `application`：账号切换、匹配、开始牌局、提交动作、离桌、恢复、结算、升级和回放等用例。
- `persistence`：按账号作用域组织 IndexedDB 表、数据迁移、导入导出和事务。
- `stores`：面向界面的状态组合，不承载扑克规则。
- `views/components`：页面和表现组件，只通过应用层发起命令。
- `styles`：设计令牌、主题、通用布局和组件样式。

核心原则：规则引擎是牌局状态的唯一真相来源，筹码账本是余额的唯一真相来源；UI 不计算合法动作、不直接改筹码、不接收 AI 难度，AI 不持有完整牌局对象。

### 5.3 事件驱动记录

每个有效操作先由规则引擎校验，再产生不可变事件；当前牌局状态和回放状态都由相同事件序列推导。建议的事件类别包括：

- 牌局创建、座位就绪、盲注投入、发底牌。
- 阶段开始、公共牌发放、轮到玩家。
- 玩家过牌、跟注、下注、加注、弃牌、全下、超时。
- 玩家请求离桌、强制弃权、筹码返还、旧桌关闭、重新匹配。
- 单手结算完成、AI 离场判定、AI 离场、空位补充和 AI 入座。
- 边池创建、摊牌、底池授予、牌局结束。
- 经验授予、等级提升、手动降级。

事件必须包含单调递增序号、时间、牌局版本和产生前的状态摘要校验值。这样既能逐行动回放，也能发现损坏或缺失的导入记录。

## 6. 关键数据对象

| 对象        | 关键内容                                                                          |
| ----------- | --------------------------------------------------------------------------------- |
| 本地账号    | 不可变 ID、唯一显示名、创建时间、最近使用时间、数据版本                           |
| 玩家档案    | 账号 ID、当前等级、当前等级经验、终身累计经验、历史最高等级、创建时间、统计摘要   |
| 筹码账本    | 账号 ID、可用余额、桌面锁定额、变动原因、关联会话、幂等键                         |
| 匹配结果    | 账号当时等级、规则版本、内部权重、AI 配置、随机种子；整体为内部数据               |
| 牌桌会话    | 账号 ID、配置、参与者、开始/结束时间、净输赢、离桌状态                            |
| AI 席位周期 | AI 身份、座位、入离场手数、入离场筹码、近期表现摘要、离场判定、补位种子和规则版本 |
| 手牌记录    | 按钮位、盲注、起始快照、事件序列、结算、经验明细                                  |
| 玩家行动    | 阶段、顺序、玩家、合法动作快照、实际动作、金额、行动后摘要                        |
| 底池        | 主池/边池、金额、合资格玩家、获胜者和分配明细                                     |
| AI 配置     | 内部难度、策略版本、参数版本、决策种子、能力开关；禁止进入玩家视图模型            |
| 等级流水    | 账号 ID、变更原因、经验差额、等级前后值、关联会话和手牌                           |
| 应用设置    | 账号 ID 或全局作用域、显示、声音、动画、回放、新手提示和数据版本                  |

## 7. 状态机与异常边界

牌桌会话阶段固定为：等待匹配、锁定买入、已入桌、阵容更新、离桌结算、重新匹配、已结束或已中止。单手牌阶段固定为：收取盲注、发底牌、翻牌前、翻牌、转牌、河牌、摊牌、结算、已结束或已中止。

需要明确处理：

- 所有剩余玩家全下后，自动发完公共牌，不再产生无意义行动。
- 只剩一名未弃牌玩家时立即结束下注并授予底池，不强制公开底牌。
- 某玩家筹码少于盲注、跟注额或最小加注额时的全下。
- 不足额全下是否重新开放加注权，严格按冻结的规则实现。
- 多个边池、多个平局、余数筹码和公共牌即最佳牌。
- 任何阶段请求离桌时，先冻结新行动，再记录强制弃权、结算底池资格和返还未投入筹码。
- 切换账号、关闭页面或连续点击不得让同一买入、返还或离桌损失结算两次。
- AI-only 旧桌结算与新桌匹配可以在界面上并行，但持久化事件顺序和筹码账本必须确定。
- 浏览器刷新、页面关闭、存储空间不足、导入记录损坏和版本不兼容。
- AI 计算异常或超时：使用同一公开信息执行可记录、可复现的保底合法动作。
- 阵容更新中刷新、关闭或事务失败：恢复到最后一次完整提交的旧阵容或新阵容，不得出现空位未补、重复补位、重复筹码流入/流出或再次随机抽取。
- 动画中快速重复点击：同一行动命令只能成功一次。

## 8. 测试与验收标准

### 8.1 规则引擎

- 同一随机种子与相同命令序列产生完全一致的牌序、事件和结算。
- 所有阶段只返回规则允许的动作和金额范围。
- 覆盖牌型比较、踢脚、平局、全下、不足额加注、主池和多边池。
- 使用固定牌例和属性测试验证筹码守恒：结算前后桌面总筹码一致。
- 任何非法动作不改变状态、不产生正式事件。

### 8.2 AI

- 测试输入中不包含对手底牌和未来牌堆。
- AI 返回的动作始终位于规则引擎给出的合法动作集合内。
- 相同输入、策略版本和种子产生相同动作。
- 难度提升有可量化的能力差异，但不承诺短期每局都更强。
- 对每个玩家等级执行大量固定种子匹配，验证三类 AI 概率落在允许误差内。
- 候选区间充足时，每桌至少覆盖两个相对难度区间；最低和最高等级不得越过各自有限窗口抽取 AI。
- 使用固定种子验证每手 AI 离场结果可复现；筹码归零必定离场，新入桌保护期生效，普通情形允许零人离场，自愿离场不超过配置上限。
- 对每个玩家等级大量模拟补位，验证新 AI 只来自该等级的候选范围，区间分布符合既有匹配权重，且不会把整桌锁定为单一难度。
- 每个离场空位恰好产生一次补位，配置的 AI 座位数保持不变；刷新恢复不会改变已经提交的离场或补位结果。
- 表现层视图模型、牌桌、记录、回放、统计和普通对局导出中均不存在 AI 难度或策略版本字段。

### 8.3 记录与回放

- 每个有效玩家行动都有且只有一条对应记录。
- 从初始快照重放全部事件所得终态与原牌局终态一致。
- 任意节点前进、后退和按阶段跳转都能恢复准确状态。
- 导出后再导入，牌局、行动、结算和经验明细保持一致。
- 损坏、缺字段或不支持版本的记录不会污染现有存档。
- 中途离桌记录包含损失、返还、强制弃权和旧桌最终结算，并能完整回放。
- 回放能够在两手之间准确显示 AI 离场和入座事件，玩家可见数据及普通导出不包含真实难度、离场评分或策略参数。

### 8.4 等级

- 未满足经验阈值不能升级，满足后只能自动升级。
- 界面不存在手动升级入口，应用层也拒绝手动升级命令。
- 只能降至不高于历史最高等级的有效等级。
- 降级清空当前等级经验，但不会篡改历史牌局、终身累计经验和历史最高等级。
- 降级后不能手动恢复到曾解锁的更高等级，后续只能靠获胜经验自动升级。
- 同一牌局不能重复领取经验，恢复或重复结算也不例外。
- 玩家等级变化只改变后续匹配概率，不直接指定下一桌 AI 难度。

### 8.5 账号、筹码与离桌

- 首次启动只自动创建一个 `admin` 账号，并能直接开始匹配。
- 两个账号的等级、经验、筹码、记录、统计、收藏和备注互不可见且不会串写。
- 重命名账号不改变数据归属；当前账号不可删除；删除其他账号只删除目标账号数据且不改变当前账号。
- 活跃牌局中切换账号不结算离桌损失；各账号活动牌局按账号 ID 隔离并独立恢复。
- 离桌后验证筹码守恒：账号返还额、已投入底池额和原桌锁定额账目完全闭合。
- 同一离桌请求重复提交、刷新恢复或异常重试，只能产生一次损失、返还和新匹配。
- 手牌中离桌不发放该会话胜利经验；两手之间离桌不损失筹码。
- 当前账号导出不包含其他账号数据，全部账号备份才能包含所有账号。

### 8.6 UI 与样式

- 模板中不存在 `style` 属性、内联样式绑定或无语义类名。
- 核心控件有明确焦点态、禁用态、加载态和键盘行为。
- 深色主题下正文、次要文字、禁用态、焦点态和操作按钮满足约定的对比度标准。
- 1920×1080、1366×768、1024×768、640×520 和 360×640 不出现控件遮挡、文字溢出、牌面重叠或核心操作横向滚动。
- 紧凑窗口和窄屏模式下可完成过牌、跟注、下注、加注、全下、弃牌和离桌重匹配的完整流程。
- 视口尺寸切换、AI 行动和动态文案变化不会让公共牌区或操作区发生不可控跳动。
- 所有下注方式遵循引擎给出的同一金额边界。
- 动画关闭或减少后，所有流程仍可完整使用。

## 9. 开发里程碑

### M0：需求冻结

- 确认桌型、会话胜负定义、筹码与盲注、经验公式、破产补充和匹配概率误差范围。
- 选定牌型评估库和 IndexedDB 封装库，核验许可证和维护状态。
- 固化规则例子与验收牌例。

### M1：无界面的领域内核

- 完成账号作用域、筹码账本、牌局状态机、合法动作、下注轮、底池和结算。
- 完成确定性洗牌与事件日志。
- 用测试覆盖正常结算与离桌重匹配下的筹码守恒和关键边界。

### M2：可玩的牌桌

- 完成深色响应式牌桌、操作区、基础 AI、账号切换和单手牌闭环。
- 完成刷新恢复、基础音效与动画设置。

### M3：记录与回放

- 完成 IndexedDB 持久化、牌局列表、详细时间线和逐行动回放。
- 完成 JSON 导入导出、版本校验和异常处理。

### M4：等级与难度

- 完成经验结算、自动升级、手动降级和等级流水。
- 完成概率匹配、分级 AI 参数、难度隐藏与公平性测试。
- 完成每手结算后的 AI 离场判定、空位补位、等级范围内随机匹配、阵容事件持久化与恢复测试。

### M5：统计与发布验收

- 完成基础统计、筛选、收藏和备注。
- 完成多账号隔离、端到端测试、小窗口/窄屏截图、无障碍检查与性能优化。

### M6：高密度牌局与策略参考

- 将模拟牌桌替换为公共牌概览和玩家列表，保留行动者、弃牌与获胜高亮。
- 完成实时 GTO 参考、悬浮摘要、二级弹窗和隐藏信息隔离测试。
- 完成桌面、640×520 小窗口和 360×640 手机竖屏适配。

## 10. 需要产品确认的决策

以下问题会直接影响规则和数据结构，建议编码前确认：

1. 首版固定 6 人桌，还是允许 2 至 6 人自由选择？
2. “获胜”按赢下一手牌、牌桌会话净盈利，还是固定手数后净盈利定义？推荐按固定手数的牌桌会话净盈利。
3. 正常会话如何结束：固定手数、达到目标盈利，还是玩家选择“带着收益离桌”？当前规则已确定中途离桌不发胜利经验，需要再确定何时算正常结束。
4. AI 数量、入桌买入和盲注是否允许自定义？自定义配置是否影响经验倍率？
5. 账号初始虚拟筹码、标准买入、最低买入以及筹码不足时如何补充？推荐提供有明确流水的最低保障筹码，避免账号永久无法开局。
6. 匹配权重采用文档中的三档初始值，还是需要更多等级段？推荐先使用三档，通过模拟结果再校准。
7. 牌局结束后是否允许查看所有 AI 的未摊牌底牌？推荐只显示规则上已公开的牌，保持 AI 信息边界一致。
8. 应用最终运行在普通浏览器、PWA，还是桌面壳中？这会影响存档备份与文件导入导出体验。

## 11. 首版完成定义

满足以下条件才视为首版完成：

- 玩家能从新建牌局开始，与 AI 连续完成约定手数并正确结算。
- 首次启动自动进入 `admin`，多个本地账号的数据完全隔离，切换账号可暂停并恢复各自唯一的活动牌局。
- 主池、边池、全下、平局和所有标准牌型均有自动化测试证明。
- 每手牌能精确记录到每个阶段、每名玩家、每次行动，并能逐行动回放。
- 获胜经验只结算一次，升级只自动发生，降级只能由玩家向下选择。
- 玩家等级只改变 AI 抽样概率，每桌保持难度多样性，所有玩家界面均隐藏真实 AI 难度。
- 每手结算后 AI 可根据剩余筹码和近期表现离场，也允许无人离场；所有空位均从玩家当前等级对应的候选难度范围中随机补齐，刷新后阵容不会重抽。
- AI 难度提升来自内部策略能力且严格看不到隐藏信息。
- 玩家可以随时离桌重匹配，已下注筹码不退、未下注筹码正确返还，重复操作不会重复结算。
- 刷新后可恢复，记录可导入导出，损坏数据有明确提示且不会覆盖正常存档。
- 项目中无内联样式，所有业务元素使用语义类名。
- 界面保持简单直接的深色风格，在标准桌面、640×520 小窗口和 360×640 窄屏下均可完成全部核心操作。

## 12. 开发执行记录

本节是后续逐项开发的唯一进度台账。每个事项必须同时记录实现结果和验证结论；涉及产品选择时登记决策，无法继续时登记暂停点。状态使用：`待开发`、`开发中`、`待决策`、`待手动验证`、`已完成`、`已阻塞`。

### 12.1 总体状态

| 记录时间   | 当前里程碑               | 状态   | 结论                                                                                         |
| ---------- | ------------------------ | ------ | -------------------------------------------------------------------------------------------- |
| 2026-09-02 | M0：需求冻结             | 已完成 | D-001 至 D-008 全部采用推荐方案；新增逐行动自动保存与防读档要求                              |
| 2026-09-02 | M1：无界面的领域内核     | 已完成 | 规则、确定性牌堆、AI 决策、账号账本、逐行动事务保存和恢复协议完成并通过自动测试              |
| 2026-09-02 | M2：可玩的牌桌           | 已完成 | 深色响应式牌桌、账号管理、操作区、AI 回合、离桌重匹配、自动恢复和牌面可读性均已通过用户验证  |
| 2026-09-02 | M3：记录与回放           | 已完成 | 逐行动回放、账号 JSON 导入导出、版本校验和异常回滚均已完成，用户已确认实际浏览器操作符合预期 |
| 2026-09-02 | M4：等级与难度           | 已完成 | 等级、经验规则、概率匹配及 AI 轮换与补位均已完成，V-004 与 V-005 均已验证通过                |
| 2026-09-02 | M5：统计与发布验收       | 已完成 | 统计、设置、发布验收及 AI 牌型亮牌边界均已完成并通过用户验证                                 |
| 2026-09-03 | M6：高密度牌局与策略参考 | 已完成 | 玩家列表、实时 GTO 参考、三档响应式、匹配说明和 AI 难度校准均已通过自动及用户验证            |

### 12.2 事项记录

| 事项                         | 状态   | 开发结果                                                                                                                                                                                             | 验证结论                                                                               | 关联决策 / 暂停点            |
| ---------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ---------------------------- |
| DEV-000：仓库与工具链核对    | 已完成 | 确认仓库当前只有方案文档和编辑器配置，尚未创建前端工程                                                                                                                                               | Node.js `22.19.0`、pnpm `11.24.0` 可用；Git 工作区干净                                 | 无                           |
| DEV-001：M0 规则冻结         | 已完成 | D-001 至 D-008 已全部按推荐方案冻结                                                                                                                                                                  | 决策覆盖桌型、胜负、会话、配置、筹码、匹配、复盘信息和运行形态                         | D-001 至 D-008；P-001 已恢复 |
| DEV-002：逐行动自动保存规则  | 已完成 | 已定义真人/AI 决策与牌局变化的事务边界、恢复语义、幂等键和多标签页约束                                                                                                                               | 规则覆盖刷新、关闭、AI 计算中断、重复提交和旧标签页并发场景                            | D-009                        |
| DEV-003：M1 工程与领域内核   | 已完成 | 创建 Vue 3/Vite/pnpm 工程，实现 domain、engine、ai、matching、persistence 和 app store                                                                                                               | `pnpm test`：15 项通过；`pnpm build`：通过                                             | 无                           |
| DEV-004：M2 可玩的牌桌       | 已完成 | 实现首页、账号管理入口、匹配配置、牌桌、下注、离桌重匹配、历史时间线和自动恢复                                                                                                                       | 自动回归和用户实际浏览器验证均通过                                                     | V-001                        |
| DEV-005：代码质量门禁        | 已完成 | 增加 ESLint、Stylelint 和 Prettier 配置，覆盖无内联样式与语义类名约束                                                                                                                                | `pnpm lint` 通过（仅 Vue 模板格式警告）；`pnpm format:check` 通过                      | 无                           |
| DEV-006：用户反馈修复        | 已完成 | 修复下一手保存异常；允许进行中牌局创建/切换账号；切回原账号自动恢复其活动牌局                                                                                                                        | `pnpm test`：15 项通过；`pnpm e2e`：5 项通过，新增下一手和账号切换回归                 | V-002                        |
| DEV-007：提示与账号管理交互  | 已完成 | 顶部提示统一缩短为 1.6 秒；头像下拉仅保留“账号管理”，账号列表、切换、删除和新建操作移入二级弹窗；当前账号不可删除                                                                                    | `pnpm test`：17 项通过；`pnpm e2e`：5 项通过；类型检查、构建、lint 和格式检查通过      | V-003                        |
| DEV-008：M3 记录、回放与备份 | 已完成 | 历史抽屉增加公共牌、玩家筹码/投入、公开底牌和逐行动前后移动；账号管理增加当前账号 JSON 导出和导入为新账号，含模式版本与事务回滚                                                                      | `pnpm test`：22 项通过；用户确认回放、备份导入导出及异常处理符合预期                   | V-004                        |
| DEV-009：M4 等级流水完善     | 已完成 | IndexedDB 升级至版本 2，新增等级流水；经验结算和手动降级分别记录来源、经验和等级变化；首页增加等级记录弹窗                                                                                           | 自动回归通过，用户确认等级记录和隐藏信息符合预期                                       | V-004                        |
| DEV-010：经验规则帮助        | 已完成 | 在首页“升级进度”旁增加帮助图标；二级弹窗展示经验结算条件、精确公式、单场上下限、升级阈值、等级影响和降级规则                                                                                         | 窄屏端到端回归通过，用户确认规则内容和可读性符合预期                                   | V-004                        |
| DEV-011：每手 AI 轮换与补位  | 已完成 | 实现最近 5 手表现与筹码离场评分、破产强制离场、每手最多 2 名自愿离场、等级概率区间补位、唯一身份、阵容事件和旧存档兼容                                                                               | 自动测试、截图和用户实际体验均通过；阵容通知、座位变化、难度隐藏及刷新恢复符合预期     | D-010；V-005；P-005          |
| DEV-012：牌面可读性优化      | 已完成 | 放大公共牌、底牌、点数和花色；桌面/小窗口公共牌为 56×80，窄屏为 44×64，底牌为 34×48；修复 640×520 操作区输入框与按钮重叠                                                                             | 三档截图和端到端回归通过，用户确认可读性优化符合预期                                   | V-006                        |
| DEV-013：每手获胜方高亮      | 已完成 | 本手结算后以绿色边框、光圈和底色高亮全部获胜座位；平分底池支持多名赢家，刷新保持，下一手清除；历史回放仅在结算帧高亮赢家                                                                             | 自动回归与三档截图通过，用户确认获胜方高亮符合预期                                     | V-007；P-006 已恢复          |
| DEV-014：统计与筛选          | 已完成 | 新增统计页和统计口径 v1；展示总手数、胜率、净输赢、摊牌率、摊牌胜率、主动入池率、翻牌前加注率及日期趋势；支持日期、位置、起手牌、当时等级、离桌状态和收藏筛选                                        | 领域单测、账号隔离、五档端到端回归和用户体验均通过                                     | V-008；P-007                 |
| DEV-015：记录收藏与备注      | 已完成 | 手牌记录支持收藏、仅收藏筛选和 500 字本地备注；列表、详情、搜索同步；仓库事务校验记录归属，备份保留标记                                                                                              | 归档、编辑、备份导出导入、跨账号隔离和用户体验均通过                                   | V-008；P-007                 |
| DEV-016：游戏设置            | 已完成 | 新增账号级设置页：动画速度、AI 思考时间、行动音效、背景音乐、音量、牌面样式、回放速度和新手提示；设置自动保存并随备份迁移；AI 延迟、回放自动播放和牌面样式已接入                                     | 类型、仓库、构建、浏览器回归和用户体验均通过                                           | V-008；P-007                 |
| DEV-017：M5 发布验收准备     | 已完成 | 完成多账号隔离、五档分辨率、无横向溢出、基础无障碍名称检查、性能纯函数基准和页面路由懒加载                                                                                                           | 39 项单测、9 条端到端流程、构建、Lint、Stylelint、格式检查和用户发布体验均通过         | V-008；P-007                 |
| DEV-018：平局结算与牌型展示  | 已完成 | 核对主池/边池平局结算；新增三方平分底池与筹码守恒测试；用户从发牌后始终展示并随公共牌动态更新，AI 仅在亮牌且未弃牌时展示                                                                             | 自动测试和用户体验均通过；平局到账、动态牌型及小窗口可读性符合预期                     | V-009；P-008                 |
| DEV-019：AI 牌型亮牌边界修复 | 已完成 | 修正 AI 牌型显示条件：仅在摊牌/结算且未弃牌时显示；用户牌型仍始终显示；补充端到端检查防止发牌、翻牌阶段泄露                                                                                          | 自动回归通过；用户确认 AI 牌型亮牌边界符合预期                                         | V-010；P-009 已恢复          |
| DEV-020：列表牌局与 GTO 参考 | 已完成 | 真实牌桌改为公共牌概览加玩家列表；新增不读取 AI 隐藏牌的 240 次未知牌模拟、权益/底池赔率/SPR、合法动作混合频率、悬浮摘要及二级弹窗                                                                   | 自动回归和三档截图通过；用户确认玩家列表、GTO 参考及竖屏体验符合预期                   | D-011；V-011；P-010 已恢复   |
| DEV-021：AI 匹配说明选项卡   | 已完成 | 升级进度帮助弹窗改为“AI 匹配 / 经验规则”双选项卡并默认打开 AI 匹配；直接复用实际匹配算法展示当前等级、当前可匹配范围、完整 1–12 档范围、三类有效概率及四组 AI 策略说明；保留具体牌桌 AI 难度隐藏边界 | 自动回归及三档截图通过；用户确认内容组织、可读性和滚动体验符合预期                     | D-012；V-012；P-011 已恢复   |
| DEV-022：AI 难度重新校准     | 已完成 | 将每级均可抽取 1–12 档改为随等级上移的有限窗口；Lv.1 收窄为 1–3 档及 85% 相近、15% 偏强，Lv.2–12 逐级提升上下界并重配比例；新匹配和每手 AI 补位共用同一规则，既有保存现场不改写                      | 55 项单测、9 条端到端流程及每级 1,200 次抽样通过；用户确认重新校准后的实际难度符合预期 | D-013；V-013；P-012 已恢复   |

| DEV-028：牌面边界与预设密度 | 已完成 | 提高 segmented 悬浮对比度；高对比度模式保持 AI 隐牌牌背并禁止信息泄漏；新增四个内置/最多 12 个自定义牌局预设，以及标准、紧凑、竖屏三档显示密度，均按账号自动保存 | 74 项单测、13 条 E2E、类型、构建、Lint、格式检查通过；四档视口与活动牌局快照验证通过 | V-018；V-019；P-019 |

### 12.3 决策记录

| 编号  | 待确认事项           | 可选方案                                                                   | 推荐方案                                                                        | 状态   | 最终决定                                          |
| ----- | -------------------- | -------------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ------ | ------------------------------------------------- |
| D-001 | 桌型                 | A. 固定 6 人桌；B. 允许 2 至 6 人选择                                      | B，默认 6 人；规则引擎仍按动态座位数设计                                        | 已决定 | B：允许 2 至 6 人，默认 6 人                      |
| D-002 | 获胜定义             | A. 赢下一手；B. 固定手数结束后净盈利；C. 自由离桌时净盈利                  | B，避免小底池刷经验，结算点确定                                                 | 已决定 | B：固定手数结束后净盈利                           |
| D-003 | 正常会话结束条件     | A. 固定 20 手；B. 达到目标盈利；C. 玩家主动带收益离桌；D. 多条件组合       | A，完成 20 手即正常结束；中途离桌继续按强制弃权处理                             | 已决定 | A：完成 20 手正常结束                             |
| D-004 | 可配置牌桌参数       | A. 全部固定；B. 仅 AI 数量可选；C. AI 数量、买入和盲注均可选               | C；经验按有效手数与净盈利比例归一化，不因参数本身加成                           | 已决定 | C：AI 数量、买入和盲注均可选                      |
| D-005 | 虚拟筹码参数         | 需要给出初始余额、标准/最低买入和余额不足规则                              | 初始 10,000；标准买入 1,000；最低买入 500；不足 500 时补至 1,000 并记录救济流水 | 已决定 | 采用推荐的 10,000 / 1,000 / 500 / 补至 1,000 规则 |
| D-006 | AI 匹配权重          | A. 使用三档权重；B. 增加更多等级段                                         | A，先采用 1-3、4-7、8+ 三档并通过模拟校准                                       | 已替代 | 初版三段权重已由 D-013 的逐级校准替代             |
| D-007 | 复盘时 AI 未摊牌底牌 | A. 全部显示；B. 只显示规则中已公开的牌                                     | B，避免破坏隐藏信息边界                                                         | 已决定 | B：只显示规则中已公开的牌                         |
| D-008 | 首版运行形态         | A. 普通浏览器；B. 可安装 PWA；C. 桌面壳                                    | A，先完成纯浏览器单机版，保留后续 PWA 能力                                      | 已决定 | A：普通浏览器单机版                               |
| D-009 | 自动保存时机         | A. 定时保存；B. 每次真人或 AI 决策及状态变化后事务保存                     | B，保证原位恢复并防止刷新读档                                                   | 已决定 | B：每次决策和状态变化后立即保存                   |
| D-010 | 每手 AI 阵容是否变化 | A. 会话内固定；B. 每手结算后按筹码和近期表现概率轮换并补齐空位             | B，允许零至多名 AI 离场；补位沿用玩家等级对应的概率区间和难度隐藏规则           | 已决定 | B：按推荐的离场评分和等级范围补位                 |
| D-011 | 实时 GTO 数据形态    | A. 引入完整求解器与预计算策略库；B. 本地公开信息近似混合策略               | B，适配动态人数、筹码和下注配置，离线可用且不显著增加首屏体积                   | 已决定 | B：采用本地公开信息近似混合策略                   |
| D-012 | AI 匹配说明公开边界  | A. 展示当前牌桌每名 AI 的实际档位；B. 仅展示全局标尺、当前匹配池和分类概率 | B，与既有难度隐藏要求一致，既解释等级影响又不泄露当前对手强度                   | 已决定 | B：公开规则与概率，继续隐藏具体对手档位           |
| D-013 | AI 难度校准方式      | A. 保留全 1–12 档候选池，仅降低偏强概率；B. 同时限制候选窗口并逐级设置比例 | B，只调概率仍可能在初始等级抽到顶级 AI；有限窗口能给实玩难度设置明确上限        | 已决定 | B：采用逐级有限窗口和逐级比例                     |

### 12.4 暂停记录

| 编号  | 时间       | 暂停位置                       | 暂停原因                                                                                  | 恢复条件                        |
| ----- | ---------- | ------------------------------ | ----------------------------------------------------------------------------------------- | ------------------------------- |
| P-001 | 2026-09-02 | M0 需求冻结，创建 Vue 工程之前 | D-001 至 D-008 会实质改变牌局状态机、经验结算、筹码账本、配置页面和存储能力，不能自行假定 | 已恢复：用户确认 D-001 至 D-008 |

### 12.5 验证记录

| 编号  | 时间       | 验证事项                                                                                   | 状态       | 结论 / 反馈                                                                                                                           |
| ----- | ---------- | ------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| V-001 | 2026-09-02 | M2 可玩的牌桌手动体验                                                                      | 已验证通过 | 用户已确认深色界面、小窗口操作、离桌重匹配、自动恢复和难度隐藏符合预期                                                                |
| V-002 | 2026-09-02 | 用户反馈：下一手报错、离桌后无法创建账号、缺少账号切换恢复                                 | 已验证通过 | 下一手、活动牌局中创建/切换账号以及切回原账号恢复现场均已通过用户验证                                                                 |
| V-003 | 2026-09-02 | 用户反馈：顶部提示时长过长、头像下拉直接展示账号列表、新建账号入口需统一、当前账号不可删除 | 已验证通过 | 已将提示时长设为 1.6 秒；下拉改为单一“账号管理”入口，账号切换、删除和新建移入二级弹窗；当前账号删除按钮禁用并由应用层和仓库层共同拒绝 |
| V-004 | 2026-09-02 | M3 逐行动回放、账号 JSON 备份及 M4 等级流水和规则界面                                      | 已验证通过 | 用户确认回放、备份、异常处理、等级记录以及经验与升级规则帮助均符合预期                                                                |
| V-005 | 2026-09-02 | 每手结算后的 AI 离场、补位、难度隐藏及刷新恢复                                             | 已验证通过 | 用户确认阵容通知、座位变化、难度隐藏及真实浏览器刷新恢复符合预期                                                                      |
| V-006 | 2026-09-02 | 牌张、点数、花色可读性及三档响应式布局                                                     | 已验证通过 | 用户确认牌面可读性优化通过                                                                                                            |
| V-007 | 2026-09-02 | 每手结算后的获胜方高亮、平分底池多赢家、刷新保持、下一手清除及回放终态高亮                 | 已验证通过 | 用户确认获胜方高亮符合预期                                                                                                            |
| V-008 | 2026-09-02 | M5 统计口径、筛选、收藏备注、账号级设置及五档发布体验                                      | 已验证通过 | 用户确认统计内容、标记保存、设置即时生效、账号隔离和小窗口操作符合预期                                                                |
| V-009 | 2026-09-02 | 平局/平分底池结算、翻牌牌型展示及用户牌型动态更新                                          | 已验证通过 | 用户确认翻牌后各座位牌型显示、用户牌型随公共牌变化、平分到账及小窗口可读性符合预期                                                    |
| V-010 | 2026-09-03 | AI 牌型仅在亮牌且未弃牌时显示                                                              | 已验证通过 | 用户确认 AI 仅在亮牌且未弃牌时显示牌型，信息边界符合预期                                                                              |
| V-011 | 2026-09-03 | 玩家列表、实时 GTO 参考及竖屏适配                                                          | 已验证通过 | 用户确认玩家列表、实时 GTO 参考、列表密度和竖屏体验符合预期                                                                           |
| V-012 | 2026-09-03 | 升级进度帮助中的 AI 匹配默认选项卡、等级区间、概率、难度介绍及竖屏适配                     | 已验证通过 | 用户确认默认选项卡、内容组织、可读性及窄屏滚动体验符合预期                                                                            |
| V-013 | 2026-09-03 | 初始等级及逐级 AI 候选窗口、匹配比例和实际对局难度                                         | 已验证通过 | 用户确认重新匹配后的初始难度、逐级候选窗口和匹配比例符合预期                                                                          |

### 12.6 自动验证结果

| 时间       | 命令 / 检查                                           | 结果                                                                                                                                            |
| ---------- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-02 | `pnpm install --registry=https://registry.npmjs.org/` | 通过；pnpm 11 构建脚本白名单已配置                                                                                                              |
| 2026-09-02 | `pnpm exec vue-tsc --noEmit -p tsconfig.app.json`     | 通过                                                                                                                                            |
| 2026-09-03 | `pnpm test`                                           | 通过；8 个测试文件、55 个测试                                                                                                                   |
| 2026-09-02 | `pnpm build`                                          | 通过；Vite 生产构建完成，Game/History/Statistics/Settings 按路由拆分，主包仍有体积提示但不影响构建                                              |
| 2026-09-02 | `pnpm e2e`                                            | 通过；9 个 Playwright 流程，覆盖破产 AI 补位、刷新恢复、统计隔离、收藏备注和设置持久化                                                          |
| 2026-09-02 | 截图检查                                              | 通过；桌面、紧凑窗口和窄屏无横向溢出，核心按钮和座位在视口内                                                                                    |
| 2026-09-02 | 牌面可读性与响应式回归                                | 通过；1366×768、640×520、360×640 牌面清晰且完整，紧凑窗口操作区无重叠                                                                           |
| 2026-09-02 | `pnpm lint`                                           | 通过；ESLint 0 错误（存在 171 条 Vue 模板风格警告），Stylelint 通过                                                                             |
| 2026-09-02 | M3/M4 回归测试                                        | 通过；备份导入为独立账号、损坏备份拒绝、回放终态一致、AI 隐藏牌边界、经验与降级流水均有覆盖                                                     |
| 2026-09-02 | `pnpm format:check`                                   | 通过；所有受检文件符合 Prettier 格式                                                                                                            |
| 2026-09-02 | 用户反馈回归测试                                      | 通过；下一手进入第 2 手，创建 second 后切回 admin 能恢复原活动牌局                                                                              |
| 2026-09-02 | 提示与账号管理回归测试                                | 通过；二级账号弹窗流程可用，删除非当前账号后当前账号指针保持不变，删除当前账号会被拒绝                                                          |
| 2026-09-02 | AI 轮换截图与跨多手模拟                               | 通过；补位通知完整可见，同桌身份唯一，连续 8 手座位数、身份和表现状态保持一致                                                                   |
| 2026-09-02 | 获胜方高亮回归                                        | 通过；结算前无高亮，结算后全部赢家高亮，刷新保持，下一手清除，历史回放仅终态显示；三档截图无重叠                                                |
| 2026-09-02 | M5 统计、标记、设置与发布回归                         | 通过；统计纯函数、记录归属、备份迁移、设置隔离、五档截图和基础控件可访问名称均有覆盖                                                            |
| 2026-09-02 | 平局结算与牌型展示回归                                | 通过；三人公共牌成顺时平分 300 底池各得 100，结算后总筹码不变；牌型映射覆盖发牌、翻牌和完整公共牌阶段                                           |
| 2026-09-03 | 列表牌局与 GTO 参考回归                               | 通过；8 个测试文件共 42 项、9 条端到端流程通过；AI 隐藏牌变化不影响策略结果，桌面/640×520/360×640 无横向溢出                                    |
| 2026-09-03 | AI 匹配说明选项卡回归                                 | 通过；8 个测试文件共 43 项、9 条端到端流程、类型检查、构建、Lint 和格式检查通过；当前等级边界概率与实际算法一致，三档截图无截断或横向溢出       |
| 2026-09-03 | AI 难度窗口与比例重新校准                             | 通过；Lv.1–12 每级 1,200 次实际抽样均未越过候选窗口且比例在误差内；55 项单测、9 条端到端流程、类型检查、构建、Lint、格式检查和 360×640 截图通过 |

### 12.7 手动验证结论

V-001 至 V-013 均已由用户在 `http://127.0.0.1:5173` 验证通过，当前没有待处理的手动验证事项。

### 12.8 暂停记录

| 编号  | 时间       | 暂停位置                                 | 原因                                                                                             | 继续条件                                                                            |
| ----- | ---------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- |
| P-002 | 2026-09-02 | M2 可玩的牌桌完成后                      | 自动化已证明功能闭环，但深色视觉、实际小窗口手感、离桌提示和隐藏难度属于需要用户判断的产品体验   | 用户完成 V-001；若通过则继续 M3 记录/回放完善与 M4 等级界面，若有问题先修复后再继续 |
| P-003 | 2026-09-02 | 用户反馈修复后，M2 再次手动验证          | 用户已指出 3 个交互问题，代码和自动化回归已修复，但视觉和真实浏览器操作仍需复验                  | 用户确认 V-002 修复有效；确认后继续 M3/M4                                           |
| P-004 | 2026-09-02 | 提示与账号管理交互完成后，M2 手动验证    | 用户确认提示、账号管理、新建账号和当前账号不可删除均符合预期，V-003 已通过                       | 已恢复：继续 M3/M4 开发                                                             |
| P-005 | 2026-09-02 | M3/M4 功能完成后的浏览器体验验证         | V-004 已通过，DEV-011 与自动验证已完成；AI 阵容变化和刷新后的直观体验仍需用户确认                | 用户完成 V-005；通过后继续 M5 统计与发布验收，发现问题则先修复并复验                |
| P-006 | 2026-09-02 | 获胜方高亮优化完成后的视觉验证           | DEV-013 功能、回放和三档响应式自动检查均通过，但实际高亮辨识度需要用户确认                       | 用户完成 V-007；与 V-005 均通过后继续 M5 统计与发布验收                             |
| P-007 | 2026-09-02 | M5 统计与发布验收后的浏览器体验验证      | DEV-014 至 DEV-017 自动验证通过；统计口径、收藏备注、设置即时生效和发布前实际操作仍需用户确认    | 用户完成 V-008；与 V-005 均通过后完成首版发布验收                                   |
| P-008 | 2026-09-02 | 平局结算与牌型展示开发完成后的浏览器验证 | 结算与牌型自动测试已通过，但翻牌时多座位牌型信息密度、用户动态牌型和真实平分到账仍需用户确认     | 用户完成 V-009；若通过则合并首版发布验收，否则根据反馈修复并复验                    |
| P-009 | 2026-09-02 | AI 牌型亮牌边界修复后的浏览器验证        | 自动化已证明发牌/翻牌阶段不泄露 AI 牌型，但真实刷新后的亮牌时机仍需用户确认                      | 用户完成 V-010；通过后恢复首版完成状态，否则根据反馈修复并复验                      |
| P-010 | 2026-09-03 | 列表牌局与 GTO 参考开发后的浏览器验证    | 自动化已覆盖布局、信息隔离和弹窗，但列表密度、竖屏滚动手感及近似策略口径仍需用户确认             | 用户完成 V-011 并确认 D-011；通过后完成 M6，否则根据反馈调整                        |
| P-011 | 2026-09-03 | AI 匹配说明选项卡开发后的浏览器验证      | 规则数据、默认选项卡和三档响应式均已自动验证，但信息层级、实际阅读体验和窄屏滚动手感需要用户判断 | 用户完成 V-012；通过后将 DEV-021 标记为已验证，否则根据反馈调整                     |
| P-012 | 2026-09-03 | AI 难度窗口与比例重新校准后的实玩验证    | 自动化可以证明范围和分布符合配置，但无法代替真人判断初始牌桌及升级后的实际难度曲线               | 用户离桌重新匹配并完成 V-013；通过后确认校准，否则根据具体等级继续微调              |

### 12.9 恢复记录

| 时间       | 恢复的暂停点   | 恢复依据                                                | 后续动作                                                        |
| ---------- | -------------- | ------------------------------------------------------- | --------------------------------------------------------------- |
| 2026-09-02 | P-001          | 用户确认全部采用推荐方案，并补充逐行动自动保存要求      | 进入 M1，连续开发至需要手动验证或出现新决策                     |
| 2026-09-02 | P-004          | 用户确认当前账号不可删除，要求 V-003 视为通过并继续开发 | 进入 M3/M4，连续开发至需要手动验证或出现新决策                  |
| 2026-09-02 | P-005          | 用户确认 V-004 和牌面可读性通过，并授权继续开发         | 完成 DEV-011 后重新停在 V-005 实际体验验证                      |
| 2026-09-02 | P-006          | 用户确认 V-007 获胜方高亮验证通过，并要求继续开发       | 进入 M5 统计与发布验收；V-005 保持待验证                        |
| 2026-09-02 | P-005（复验）  | 用户确认 V-005 AI 轮换、补位及刷新恢复验证通过          | 恢复并完成 M5 统计、设置和发布验收                              |
| 2026-09-02 | P-007          | 用户确认 V-008 统计、收藏备注、设置及发布体验通过       | 恢复并完成平局结算与牌型展示验收                                |
| 2026-09-02 | P-008          | 用户确认 V-009 平局结算与牌型展示体验通过               | 首版完成定义全部满足，当前无暂停事项                            |
| 2026-09-03 | P-009 至 P-012 | 用户确认 V-010、V-011、V-012、V-013 全部验证通过        | 关闭全部当前暂停点，M5、M6 及 DEV-019 至 DEV-022 标记为已完成   |
| 2026-09-04 | P-013          | 用户确认 D-014 至 D-022 全部采用推荐方案 B              | 恢复 1.1.0 阶段 A，先开发 F-001 复盘增强，再开发 F-002 本地训练 |
| 2026-09-04 | P-014          | 用户确认 V-014 复盘增强体验验证通过                     | DEV-023 标记为已验证，继续 F-002 本地训练开发                   |
| 2026-09-04 | P-015          | 用户确认 V-015 本地训练体验验证通过，并提出答题反馈优化 | DEV-024 核心功能标记为已验证；增加正确答案高亮和推荐原因后复验  |
| 2026-09-04 | P-016          | 用户确认 V-016 训练答题反馈复验通过                     | DEV-025 标记为已验证，阶段 A 完成，进入阶段 B F-003             |
| 2026-09-04 | P-017          | 用户确认 V-017 本地备份与存储管理验证通过               | DEV-026 标记为已验证，继续阶段 B F-004 本地规则教学中心         |

## 13. 1.1.0 版本开发事项

本章定义 1.1.0 版本的候选需求、实施规格和验收方式。1.1.0 已完成 13.9 决策；阶段 A 已通过验证，当前进入阶段 B，按 13.4 的顺序逐项评估、开发和验证。未通过用户确认的事项不视为已承诺交付。后续开发仍必须遵守以下边界：

- 保持纯单机，核心牌局、AI、训练、统计和存档均可在无网络状态下运行。
- 不增加充值、提现、支付、广告、订阅、抽卡、付费皮肤或任何内购。
- 不引入云端账号、云同步、在线排行榜或联网匹配；跨设备迁移继续使用用户主动导出的本地备份。
- 新功能不能破坏现有自动保存、确定性回放、账号隔离、AI 难度隐藏和虚拟筹码规则。
- 涉及正式等级、经验和筹码的功能必须防止重复领取；训练和测试模式默认不影响正式进度。

### 13.1 P0：优先开发

#### F-001 复盘增强

- 牌局结束后展示总投入、总赢取、净输赢、最大底池、最大单手输赢、主动入池率、加注率、跟注率、弃牌率、摊牌率和摊牌胜率。
- 自动标记大额底池、全下、河牌高额下注、赔率极限跟注和可能改变结果的关键决策。
- 在任意决策节点提供“其他合法行动模拟”，使用同一牌堆和公开信息推演替代路线；模拟结果不得修改正式记录、筹码或经验。
- 复盘中增加权益、底池赔率和有效筹码变化曲线，明确标注估算值。
- 手牌支持本地标签和备注，标签可参与历史筛选。

#### F-002 本地训练模式

- 随机局面训练：从历史牌局或固定种子生成一个决策节点，隐藏后续牌面和结果。
- 翻牌后、转牌后、河牌后专项训练。
- 底池赔率和最低跟注权益训练。
- 起手牌、位置和前置行动训练。
- 支持本地固定种子的 10 题或 20 题挑战，记录正确率、用时和错误类型。
- 训练模式不得发放正式经验，不得改变正式筹码和等级。

#### F-003 本地备份与存储管理

- 自动保留最近 3 个本地快照；导入前自动创建回滚快照。
- 展示备份中的账号数、牌局数、记录时间和数据版本。
- 支持只恢复记录、只恢复设置或完整恢复。
- 显示 IndexedDB 使用量和记录数量，达到阈值时提前提示。
- 支持按时间清理回放、清理指定账号或清理统计缓存，删除前必须展示影响范围。
- 存储失败时进入只读保护，不允许继续推进未保存的牌局状态。

#### F-004 本地规则教学中心

- 解释牌局流程、位置、按钮位、主池、边池、全下、不足额加注、底池赔率、有效筹码、摊牌和余数筹码。
- 从操作区、GTO 参考和复盘页面可跳转到相关术语。
- 教学内容以静态本地资源提供，不依赖网络。

### 13.2 P1：体验与长期可玩性

#### F-005 AI 行为风格

- 为 AI 增加紧手、松手、激进、被动、波动和适应型等行为特征。
- 行为风格不得与真实难度固定绑定，不能通过姓名、头像、延迟或台词反推出 AI 档位。
- 牌局记录只展示公开行为，不展示 AI 内部评分、参数或策略版本。

#### F-006 行为解释与决策提示

- 对下注尺度、底池赔率变化、价值下注、连续弃牌和高波动行动提供事实性解释。
- 不输出“AI 一定知道某张牌”或其他无法由公开信息证明的结论。
- 解释不能替代用户决策，也不能修改正式牌局结果。

#### F-007 成就与本地挑战

- 增加完成牌局、平分底池、赢下边池、完成复盘、特定牌型和连续完成手数等成就。
- 增加盈利、控制亏损、指定盲注、指定行动限制等本地挑战。
- 奖励仅使用徽章、头像框、牌面样式或配色等非货币化内容，不奖励可刷取的正式筹码。

#### F-008 牌局预设与显示密度

- 保存 AI 数量、买入、盲注、动画、音效和回放速度为本地预设。
- 提供标准、紧凑、竖屏三种显示密度，只改变表现层，不改变规则。
- 继续覆盖 1366×768、1024×768、640×520 和 360×640。

#### F-009 操作效率与误触保护

- 桌面端支持弃牌、过牌/跟注、下注/加注、全下、继续和 GTO 入口快捷键。
- 移动端支持安全区域、长按确认高风险动作和操作区固定可达。
- 全下、大额下注、离桌和删除操作支持可配置的二次确认。
- 所有提交按钮必须具备幂等和短暂锁定，避免快速重复点击。

#### F-010 PWA 离线安装

- 支持添加到桌面、离线启动、静态资源缓存和版本更新提示。
- 不缓存或依赖远程 AI、远程账号和远程数据。
- 离线状态下所有核心牌局和存档功能保持可用。

#### F-011 公开数据导出

- 在现有 JSON 备份之外，增加统计 CSV、单手 Markdown 和纯文本行动记录。
- 公开导出必须移除 AI 难度、策略参数和未公开底牌。
- 恢复型 JSON 与分享型导出必须明确区分，避免误覆盖本地数据。

### 13.3 P2：长期建设

#### F-012 规则测试沙盒

- 允许指定固定牌面、公共牌、筹码、下注历史、平局和边池场景。
- 测试沙盒不产生正式记录、经验、统计和等级变化。
- 可用于回归复杂结算、全下、平局和余数筹码。

#### F-013 AI 公平性诊断

- 在开发诊断页面统计各等级候选范围、抽样分布、行动比例、响应时间、离场率和长期结果。
- 仅展示聚合数据，不显示当前牌桌 AI 实际难度。
- 用于校准难度曲线，不作为玩家奖励或惩罚依据。

#### F-014 无障碍增强

- 完整键盘导航、稳定焦点顺序、弹窗自动聚焦和屏幕阅读器描述。
- 牌面、行动状态和输赢不只依赖颜色表达。
- 增加色弱友好的花色图标和高对比度牌面。
- 动画、音效、背景音乐和提示分别可控。

#### F-015 本地阶段与里程碑

- 将等级成长补充为新手、稳定、进阶、熟练、专家等本地阶段。
- 展示阶段手数、阶段胜率、阶段净盈利、历史最高等级和挑战完成度。
- 不做在线排行榜，不制造每日登录或付费竞争压力。

### 13.4 推荐开发顺序

1. F-001 复盘增强。
2. F-002 本地训练模式。
3. F-003 本地备份与存储管理。
4. F-004 本地规则教学中心。
5. F-008 牌局预设与显示密度。
6. F-009 操作效率与误触保护。
7. F-005 AI 行为风格和 F-006 行为解释。
8. F-007 成就与本地挑战。
9. F-010 PWA 离线安装。
10. F-011 至 F-015 按用户反馈和维护成本择机开发。

### 13.5 明确不纳入需求池

- 联机匹配、好友房、观战、在线排行榜和云端赛季。
- 充值、提现、支付、广告、订阅、抽卡、付费皮肤和真钱相关功能。
- 云同步、远程账号认证和依赖网络的 AI 推理。
- 通过每日登录、限时奖励或付费道具强迫用户持续游玩。

### 13.6 后续开发通用实施约束

后续每个功能都必须按“需求冻结 → 领域实现 → 持久化 → 表现层 → 自动测试 → 浏览器验证 → 文档记录”的顺序执行。没有完成对应验收记录，不得把事项标记为已完成。

#### 13.6.1 代码边界

- 扑克规则、牌型、底池、赔率和训练评分放在 `src/domain`，使用纯函数；不得在 Vue 组件中重新计算规则。
- IndexedDB 读写、数据迁移、导入导出放在 `src/persistence`；所有账号私有数据都必须包含 `accountId`。
- 跨页面状态放在 `src/stores`；页面只发起命令，不直接修改牌局、筹码、等级或训练结果。
- Vue 页面和组件只负责展示、输入和事件派发；新增可见元素必须使用语义类名，禁止内联样式和模板样式绑定。
- 新增规则配置必须集中在对应领域模块并带 `version`，禁止在 `HomeView.vue`、`GameView.vue` 或测试中散落魔法数字。
- 任何涉及 AI 的模块都只能接收脱敏公开状态和自身允许读取的信息；表现层不得拿到 AI 难度字段。

#### 13.6.2 数据隔离和模式

- 正式牌局数据、训练数据、测试沙盒数据使用不同的记录类型或表，不允许通过布尔字段模糊区分。
- 训练和沙盒记录必须包含 `mode: "training" | "sandbox"`，正式牌局为 `mode: "cash"`；只有 `cash` 才能产生筹码、经验、等级流水和正式统计。
- 新增数据结构必须增加模式版本、创建时间、更新时间和所属账号；删除或迁移必须可回滚。
- 任何“重新计算”只能生成新的派生结果，不得覆盖原始事件、原始行动和正式结算。
- 所有写入仍遵守现有“先持久化、后推进”原则；写入失败时停止推进并显示可恢复错误。

#### 13.6.3 测试和验收模板

每个后续事项至少要有：

- 领域单测：正常流程、边界值、非法输入、确定性种子和重复执行。
- 持久化测试：账号隔离、模式隔离、迁移、导入导出和事务失败回滚。
- E2E 测试：核心用户路径、刷新恢复、重复点击、窄屏布局和隐藏信息边界。
- 截图或尺寸断言：`1366×768`、`1024×768`、`640×520`、`360×640`。
- 文档记录：事项编号、实现结果、自动验证命令、手动验证结论、决策和暂停点。

### 13.7 分阶段详细开发规格

#### 阶段 A：复盘和训练基础（F-001、F-002）

**A1：复盘统计和关键决策**

- 输入：现有 `HandHistoryRecord`、逐行动事件、底池结果、玩家公开状态。
- 新增领域纯函数：
  - `buildHandReviewSummary(record)`：生成固定口径的单手摘要。
  - `findKeyDecisions(record)`：按底池占比、全下、河牌大额下注和赔率阈值标记节点。
  - `buildEquityTimeline(record)`：只使用当时可见牌和公开状态生成估算曲线。
- 新增数据：
  - 关键节点编号、触发原因、阶段、底池占比、用户行动。
  - 权益、底池赔率、有效筹码等估算值必须注明 `estimated: true`。
- 页面：
  - 历史详情增加“摘要 / 关键决策 / 权益曲线 / 行动时间线”分区。
  - 关键决策点击后定位到对应回放节点。
  - 增加“模拟其他行动”按钮，模拟结果使用独立样式并明确“不会修改正式牌局”。
- 验收：
  - 同一记录重复计算结果完全一致。
  - 模拟路线不改变账号筹码、经验、正式统计和原始事件。
  - AI 隐藏牌不参与玩家可见权益计算。

**A2：训练模式**

- 训练入口：首页新增“训练”入口，正式牌桌进行中时可进入，但不能覆盖活动牌局。
- 局面来源：
  - 历史局面：从用户已完成的 `cash` 牌局抽取决策节点。
  - 固定种子局面：使用本地训练种子生成，不调用网络。
- 训练流程：
  1. 展示底牌、公共牌、位置、底池、下注、有效筹码和合法动作。
  2. 用户提交动作和可选下注额。
  3. 锁定答案，展示底池赔率、估算权益、参考混合频率和解释。
  4. 记录正确性、耗时、错误类型，进入下一题。
- 新增数据：
  - `TrainingSession`、`TrainingQuestion`、`TrainingAnswer`，均带账号 ID 和训练模式版本。
  - 不写入 `accounts.bankroll`、`progression`、正式 `handRecords`。
- 验收：
  - 关闭浏览器后可恢复训练进度，但不能恢复成可领取经验的正式牌局。
  - 10 题和 20 题挑战结束后只产生训练统计。
  - 非法动作和重复提交不会产生第二条答案。

#### 阶段 B：存档、规则和设置效率（F-003、F-004、F-008、F-009）

**B1：本地备份和存储管理**

- 新增 `BackupSnapshot` 元数据：快照 ID、账号范围、数据版本、创建时间、记录数量、校验摘要。
- 导出前先生成快照；导入前自动保存当前状态；导入失败只回滚本次导入。
- 恢复选项必须明确区分：
  - 完整恢复：账号、牌局、记录、设置。
  - 记录恢复：只恢复历史和回放。
  - 设置恢复：只恢复显示、声音和动画设置。
- 存储管理页面展示估算占用、账号数量、活动牌局数量和可清理项。
- 清理动作必须二次确认，且当前活动牌局和最近备份不可被无提示删除。
- 验收：损坏备份、版本过低、同名账号、导入中断和空间不足都有可读错误且原数据不丢失。

**B2：规则教学中心**

- 内容采用本地静态 `src/content/rules` 资源，不从远程加载。
- 每个术语包含唯一 ID、标题、简短解释、详细解释、示例和关联页面入口。
- 首批术语：牌局流程、位置、按钮位、主池、边池、全下、不足额加注、底池赔率、有效筹码、摊牌、平局和余数筹码。
- Game、GTO、History 页面只能通过术语 ID 打开教学内容，避免复制多份规则文字。
- 验收：离线打开、窄屏阅读、键盘关闭弹层和从关联入口返回均可用。

**B3：牌局预设和显示密度**

- 预设只保存用户配置，不保存 AI 实际难度和当前牌局状态。
- 预设字段：AI 数量、买入、盲注、动画速度、AI 思考时间、音效、回放速度、新手提示和显示密度。
- 内置预设：标准、快速练习、深筹码、复盘；用户可新建、重命名、复制和删除自定义预设。
- 显示密度只影响 CSS 状态类和可见字段，不改变引擎计算。
- 验收：切换密度不改变牌局快照，四档视口无横向溢出，操作区始终可达。

**B4：快捷键和误触保护**

- 默认快捷键：`F` 弃牌、`C` 过牌/跟注、`B` 下注/加注、`A` 全下、`Space` 下一手、`G` GTO、`Esc` 关闭弹层。
- 输入框、弹窗、下拉选项聚焦时暂停牌局快捷键，避免误触。
- 全下、大额下注和离桌可配置确认；确认文案必须显示动作和筹码影响。
- 提交按钮在命令完成或失败前锁定，命令带会话 ID、行动序号和幂等键。
- 验收：鼠标、键盘、触屏各完成一条完整牌局路径；连续点击只生成一条事件。

#### 阶段 C：AI 行为和解释（F-005、F-006）

- AI 行为风格作为内部配置注入 `decideAiAction`，不改变公共 `AiProfile` 返回结构。
- 风格参数必须可序列化、可复现、可测试；同一局面、种子和版本输出相同动作。
- 风格与难度独立抽样，名称、头像、延迟和台词不得固定映射到风格或难度。
- 解释只基于公开输入和已执行动作，新增纯函数 `explainPublicAction(action, publicState)`。
- 解释分为事实、指标和提示三类，不输出未公开底牌推断，不替代用户决策。
- 验收：
  - AI 仍只返回合法动作。
  - 隐藏底牌变化不会改变基于公开信息生成的解释。
  - 记录、回放、普通导出不包含 AI 风格参数和难度字段。

#### 阶段 D：成就、挑战和本地阶段（F-007、F-015）

- 成就定义为版本化静态配置：ID、名称、描述、触发事件、是否可重复、展示徽章。
- 触发采用事件汇总器，不在每个页面单独判断；同一事件重复写入必须幂等。
- 挑战分为牌局目标、行动约束、盈利目标和复盘目标；每个挑战有本地固定种子或明确数据范围。
- 成就奖励仅为徽章、头像框、牌面样式或配色；不得奖励正式筹码、经验倍率或付费内容。
- 本地阶段只读取已存在的等级、手数、胜率、净盈利和挑战完成度，不改变等级升级公式。
- 验收：重复刷新不重复解锁，删除或导入账号时成就按账号隔离，训练和沙盒事件不触发正式成就。

#### 阶段 E：PWA、导出和诊断（F-010、F-011、F-013、F-014）

- PWA 只缓存构建产物和本地资源；Service Worker 更新采用“下次启动提示更新”，不得后台覆盖活动牌局。
- CSV、Markdown 和纯文本导出均为分享格式；JSON 仍是唯一恢复格式。
- AI 公平性诊断仅在高级设置或开发模式展示聚合结果，数据来源为本地日志和固定种子模拟。
- 无障碍验收覆盖键盘焦点、弹窗焦点、ARIA 名称、状态播报、色弱对比度和减少动画设置。
- 验收：断网启动、断网完成一手牌、更新中刷新、导出后重新导入和屏幕阅读器基本流程均通过。

#### 阶段 F：规则测试沙盒（F-012）

- 沙盒入口与正式牌局、训练入口分离，并在页面顶部持续显示“测试沙盒”。
- 输入支持固定牌堆、底牌、公共牌、座位、筹码、按钮位、盲注和行动序列。
- 沙盒只能调用规则引擎和结算函数，不调用账号账本、经验结算、AI 轮换或正式记录仓库。
- 支持保存和导出沙盒案例，案例数据不进入正式备份恢复范围，除非用户显式选择“导出测试案例”。
- 验收：主池、多个边池、全下、平局、余数筹码和非法行动均可复现；退出沙盒后正式账号数据不变化。

### 13.8 后续事项的暂停和决策规则

- 每个阶段开始前先确认是否采用推荐方案；涉及数据结构、正式进度、存档兼容或用户可见规则时必须新增 `D-xxx` 决策记录。
- 每个阶段完成自动验证后，若仍需要用户判断视觉、操作手感、难度或规则解释，新增 `V-xxx` 和 `P-xxx`，暂停开发直到用户确认。
- 用户确认后必须在同一轮更新事项状态、验证结论、暂停恢复记录和文档顶部阶段状态。
- 未经用户确认的候选需求不能写入“首版完成定义”，也不能改变当前已验证功能的默认行为。

### 13.9 1.1.0 开发前待用户判断事项

以下问题会改变 1.1.0 的数据结构、页面范围或开发顺序，需要在开始阶段 A 前冻结。每项已给出推荐方案；用户可以直接回复“全部采用推荐方案”，也可以只修改某一项。

| 编号  | 需要判断的内容     | 方案 A                                                 | 方案 B（推荐）                                                             | 影响范围                                             |
| ----- | ------------------ | ------------------------------------------------------ | -------------------------------------------------------------------------- | ---------------------------------------------------- |
| D-014 | 复盘模拟范围       | 只模拟用户在原节点的替代动作，不允许继续推演到手牌结束 | 同一牌堆和公开信息下完整推演替代路线，单独保存模拟结果，不影响正式数据     | F-001 的引擎接口、计算耗时、结果展示和存储结构       |
| D-015 | 训练题目来源       | 只使用固定种子题库，所有用户题目一致                   | 固定种子题库 + 当前账号已完成牌局抽题；没有历史记录时自动回退固定题库      | F-002 的题目生成、账号隔离和首次使用体验             |
| D-016 | 备份快照保留数量   | 仅保留最近 1 个快照，节省空间                          | 默认保留最近 3 个快照，可在设置中调整为 1、3、5 个                         | F-003 的 IndexedDB 表、清理界面和存储占用提示        |
| D-017 | 预设与显示密度范围 | 只保存牌局参数，不增加显示密度模式                     | 牌局参数与标准/紧凑/竖屏显示密度一起保存；密度只改变视图，不改变规则       | F-008 的设置结构、响应式样式和四档视口验收           |
| D-018 | AI 行为风格可见性  | 在牌局中显示“紧手/激进”等风格标签，方便用户理解对手    | 牌局中不显示风格名称，只在复盘中以公开行为解释呈现；风格仍不与难度固定绑定 | F-005、F-006 的信息隐藏边界和 UI 文案                |
| D-019 | 成就奖励类型       | 成就可奖励少量正式筹码或经验                           | 只奖励徽章、头像框、牌面样式和配色等非数值内容，不改变正式经济和等级曲线   | F-007 的账号数据、反刷机制和长期平衡                 |
| D-020 | PWA 是否纳入 1.1.0 | 1.1.0 只做浏览器功能，PWA 延后到后续版本               | 1.1.0 纳入离线安装、静态缓存和更新提示，但不做云同步                       | F-010 的构建配置、Service Worker、更新策略和断网验收 |
| D-021 | 规则沙盒目标用户   | 只面向开发测试，不在普通用户导航中展示                 | 面向普通用户提供“测试沙盒”，但明确不产生正式记录、经验、统计和筹码         | F-012 的入口、权限边界、案例保存和数据隔离           |
| D-022 | 1.1.0 首批交付范围 | 一次性开发 F-001 至 F-015，再统一验收                  | 先交付阶段 A（F-001、F-002），验证复盘和训练价值后再进入阶段 B             | 版本周期、暂停点数量、用户反馈对后续需求的影响       |

#### 决策结论

用户于 2026-09-04 确认“全部按照推荐方案”。D-014 至 D-022 全部采用方案 B；首批交付范围为阶段 A（F-001、F-002），阶段 B 及之后的功能在阶段 A 验证通过后继续。

#### 当前暂停点

| 编号  | 时间       | 暂停位置         | 暂停原因                                                                                   | 继续条件                                                   |
| ----- | ---------- | ---------------- | ------------------------------------------------------------------------------------------ | ---------------------------------------------------------- |
| P-013 | 2026-09-04 | 1.1.0 开发开始前 | F-001 至 F-015 会新增训练、模拟、备份、成就和 PWA 等数据与页面边界，部分选择会改变整体架构 | 已恢复：用户确认 D-014 至 D-022 全部采用方案 B，进入阶段 A |

### 13.10 1.1.0 开发记录

#### DEV-023：F-001 复盘增强（阶段 A）

- 状态：已完成并经用户验证通过。
- 实现：新增 `src/domain/review.ts`，提供单手摘要、关键决策标记、公开信息权益/底池赔率/SPR 时间线及替代行动模拟；历史详情抽屉增加复盘摘要、关键决策和估算曲线展示。
- 数据隔离：新增 IndexedDB `reviewSimulations` 表，模拟结果按账号和手牌记录独立保存；不修改正式手牌事件、筹码账本、经验、等级或统计。
- 信息边界：权益计算只使用用户底牌、当时已公开公共牌和未弃牌对手数量；AI 未亮牌底牌不会进入用户可见计算。
- 自动验证：`node_modules\\.bin\\vue-tsc.cmd --noEmit -p tsconfig.app.json`、`node_modules\\.bin\\vitest.cmd run`（9 个测试文件、57 项通过）、`node_modules\\.bin\\vite.cmd build`、`node_modules\\.bin\\eslint.cmd src tests`、`node_modules\\.bin\\stylelint.cmd \"src/**/*.scss\"` 均通过。

#### V-014：F-001 复盘体验验证（已验证通过）

- 验证范围：历史页打开已完成手牌；查看摘要数值、关键决策定位、权益时间线；点击“模拟其他行动”并刷新/切换账号，确认模拟结果仍在原账号且正式筹码、经验、统计和原始行动不变。
- 信息边界：在 AI 未亮牌的回放节点，确认权益和模拟结果没有显示或使用 AI 隐藏底牌；仅在结算公开节点显示已亮牌信息。
- 响应式：检查 `1366×768`、`1024×768`、`640×520`、`360×640`，确认复盘抽屉无横向溢出、关键决策按钮可达、权益列表可滚动。
- 结论：用户于 2026-09-04 确认验证通过；复盘内容、信息边界和响应式体验符合预期。

#### 当前暂停点

| 编号  | 时间       | 暂停位置               | 暂停原因                                                                           | 继续条件                                                             |
| ----- | ---------- | ---------------------- | ---------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| P-014 | 2026-09-04 | DEV-023 自动验证完成后 | 复盘摘要、估算曲线和替代行动模拟涉及信息密度、数值解释和窄屏操作体验，需要用户确认 | 已恢复：用户确认 V-014 通过，DEV-023 已验证，进入 F-002 本地训练开发 |

#### DEV-024：F-002 本地训练模式（阶段 A）

- 状态：核心功能已完成并经用户验证通过；正在优化答题反馈。
- 实现：新增版本化训练领域模型 `src/domain/training.ts`，支持固定种子题库、当前账号已完成 cash 牌局抽题、10/20 题挑战、合法动作校验、下注尺度校验、答案锁定、正确率/用时/错误类型记录和公开信息解释。
- 页面：新增 `/training` 路由和首页“决策训练”入口；训练页展示底牌、公共牌、位置、底池、跟注额、有效筹码、合法动作、参考混合频率和估算权益。
- 数据隔离：新增 IndexedDB `trainingSessions` 表；训练开始、提交答案、进入下一题均先保存后推进；刷新后恢复当前账号训练进度；训练不会写入正式筹码、经验、等级、cash 手牌记录或正式统计。
- 并存规则：正式牌局进行中允许进入训练，训练不能覆盖或改变 `activeGames` 中的正式牌局现场。
- 自动验证：`node_modules\\.bin\\vue-tsc.cmd --noEmit -p tsconfig.app.json`、`node_modules\\.bin\\vitest.cmd run`（10 个测试文件、61 项通过）、`node_modules\\.bin\\vite.cmd build`、`node_modules\\.bin\\eslint.cmd src tests e2e`、`node_modules\\.bin\\stylelint.cmd \"src/**/*.scss\"` 和 `node_modules\\.bin\\playwright.cmd test`（10 条流程）均通过。

#### V-015：F-002 本地训练体验验证（已验证通过）

- 验证范围：首页进入“决策训练”，分别开始 10 题和 20 题挑战；确认题目展示底牌、公共牌、位置、底池、跟注额、有效筹码和合法动作。
- 提交流程：选择动作并提交；确认重复提交被锁定，下注/加注金额越界会提示，提交后显示正确性、错误类型、估算权益、底池赔率、参考混合频率、解释和用时。
- 恢复与隔离：提交一题后刷新浏览器，确认当前题目和答案状态恢复；正式牌局进行中进入训练，再回到牌局确认行动序号、牌面、筹码和自动保存现场不变；切换账号确认训练进度互不串号。
- 完成挑战：完成 10 题或 20 题后确认只产生训练统计，不增加正式经验、等级、筹码、cash 对局记录或正式统计。
- 响应式：检查 `1366×768`、`1024×768`、`640×520`、`360×640`，确认训练页面无横向溢出，牌面、动作按钮、下注输入和“下一题”始终可达。
- 结论：用户于 2026-09-04 确认训练核心流程验证通过，并要求提交答案后高亮正确答案、写明推荐原因。

#### 当前暂停点

| 编号  | 时间       | 暂停位置               | 暂停原因                                                           | 继续条件                                                            |
| ----- | ---------- | ---------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------- |
| P-015 | 2026-09-04 | DEV-024 自动验证完成后 | 训练题目难度、参考策略解释、提交反馈和窄屏操作手感需要用户实际判断 | 已恢复：用户确认 V-015 通过；按反馈补充正确答案高亮和推荐原因后复验 |

#### DEV-025：训练正确答案与推荐原因

- 状态：已完成并经用户验证通过。
- 实现：提交答案后在原动作按钮中标记并高亮参考频率最高的“正确答案”；答错时同步标记“你的选择”，同时在反馈区显示推荐动作名称和具体原因。
- 判定口径：正确答案继续复用训练参考混合频率的最高项；若存在并列最高频率则全部标记。推荐原因由领域层根据估算权益、底池赔率、有效筹码和动作类型生成，不在页面重复评分规则。
- 恢复行为：答案状态、正确答案高亮和推荐原因均由已保存训练会话重建，刷新后保持一致。
- 自动验证：类型检查通过；10 个测试文件共 62 项通过；生产构建、Stylelint、本次变更文件 ESLint 和 Prettier 检查通过；Playwright 10 条流程全部通过，覆盖错误选择、正确答案标识、推荐原因及刷新恢复。

#### V-016：训练答题反馈复验（已验证通过）

- 提交正确答案：确认对应动作显示“正确答案”，且反馈区说明推荐该动作的原因。
- 提交错误答案：确认错误动作显示“你的选择”，参考动作显示“正确答案”，两者不只依靠颜色区分。
- 刷新与窄屏：提交后刷新确认标识和原因仍在；在 `360×640` 下确认按钮文字、标识和原因完整可读且无横向溢出。
- 结论：用户于 2026-09-04 确认验证通过；正确答案、用户误选和推荐原因的呈现符合预期。

#### 当前暂停点

| 编号  | 时间       | 暂停位置               | 暂停原因                                             | 继续条件                                                   |
| ----- | ---------- | ---------------------- | ---------------------------------------------------- | ---------------------------------------------------------- |
| P-016 | 2026-09-04 | DEV-025 自动验证完成后 | 正确答案高亮辨识度及推荐原因的实际可读性需要用户确认 | 已恢复：用户确认 V-016 通过，阶段 A 完成，进入阶段 B F-003 |

#### DEV-026：F-003 本地备份与存储管理（阶段 B）

- 状态：已完成并经用户验证通过。
- 数据结构：IndexedDB 升级至版本 6，新增独立的 `backupSnapshots` 表；恢复型 JSON 使用数据版本、导出时间、数据清单和校验和验证文件完整性，不与账号分享备份混用。
- 备份与恢复：新增 `/storage` 全局存储管理页，可备份本机全部账号、活动牌局、手牌、账本、等级进度、复盘模拟、训练和设置；支持完整恢复、只恢复记录、只恢复设置。局部恢复要求备份中的账号 ID 已存在，只替换备份内账号的数据，并保留备份后新建账号的数据。
- 回滚保护：默认保留最近 3 个快照，可调整为 1、3、5；导出前、导入前和记录清理前自动创建快照，列表明确显示来源；导入先校验再修改，恢复写入使用单个事务，失败不保留部分数据。
- 存储与清理：概览统计全部本地业务表和快照的估算体积、账号数、活动牌局数、手牌数、训练数及浏览器配额比例；达到 80% 显示预警。可选择任一账号和截止日期预览、清理旧手牌、关联复盘模拟及已完成训练；活动牌局、未完成训练、账号、筹码、经验和快照不删除。统计由历史记录实时计算，没有独立统计缓存。
- 只读保护：真人或 AI 决策、下一手及离桌重匹配写入失败时锁定正式牌局推进；重新载入最后成功保存的本地现场后才解除。牌桌操作按钮同步禁用，存储页提供恢复入口。
- 响应式：存储概览、导入清单、恢复范围、快照列表、账号清理控件在 `1366×768`、`1024×768`、`640×520`、`360×640` 下重排，无内联样式，新增元素均使用语义类名。
- 自动验证：`vue-tsc` 通过；11 个测试文件共 68 项单测通过，其中 6 项覆盖备份清单、校验、完整/记录/设置恢复、跨账号保留、快照和安全清理；Vite 生产构建、Stylelint、变更文件 Prettier 和 ESLint 0 错误通过。Playwright 11 条流程全部通过，存储流程覆盖快照保留、JSON 下载与重新读取、四档视口无横向溢出及活动牌局现场不变。

#### V-017：F-003 本地备份与存储管理体验验证（已验证通过）

- 入口与概览：在有活动牌局时打开顶部“存储”，确认账号、活动牌局、手牌、训练、估算占用和浏览器配额信息可读，返回牌局后现场未改变。
- 快照：分别选择保留 1、3、5 个并连续创建快照，确认只保留设定数量；确认手动、导出前、导入前、清理前来源显示正确；恢复和删除均有二次确认。
- 导出与校验：导出 JSON 后重新选择该文件，确认文件名、导出时间、账号数、手牌数、活动牌局数和数据版本可见；修改 JSON 任意内容后重新选择，确认校验失败且本机数据不变。
- 恢复范围：使用测试账号分别验证“只恢复设置”“只恢复记录”“完整恢复”；确认局部恢复不覆盖账号、筹码、等级和活动牌局，并保留备份后新建账号的数据；完整恢复回到备份时的全部账号和现场。
- 清理：选择测试账号和日期，确认预览数量与实际删除一致；活动牌局、未完成训练、筹码、经验及其他账号记录不变，操作后出现“清理前”快照。
- 响应式：检查 `1366×768`、`1024×768`、`640×520`、`360×640`，确认导入范围、快照按钮、账号选择、日期和清理按钮无重叠、截断或横向滚动。
- 结论：用户于 2026-09-04 确认验证通过；全局备份恢复、快照、指定账号清理及四档响应式体验符合预期。

#### 当前暂停点

| 编号  | 时间       | 暂停位置               | 暂停原因                                                                                           | 继续条件                                                          |
| ----- | ---------- | ---------------------- | -------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| P-017 | 2026-09-04 | DEV-026 自动验证完成后 | 备份恢复和记录清理会实际改写本地数据，快照来源辨识度、二次确认、四档布局及真实恢复体验需要用户确认 | 已恢复：用户确认 V-017 通过，进入阶段 B 的 F-004 本地规则教学中心 |

#### DEV-027：F-004 本地规则教学中心（阶段 B）

- 状态：开发与自动验证已完成，等待 V-018 人工验证。
- 本地知识库：新增类型化静态规则数据，不请求网络、不新增持久化表；包含牌局流程、位置与按钮位、行动与下注轮、主池与边池、全下、不足额加注、底池赔率、有效筹码与 SPR、摊牌与牌型比较、余数筹码共 10 个主题。每个主题均包含摘要、规则段落、实例或要点、搜索关键词及有效的相关主题链接。
- 教学中心：新增 `/rules` 懒加载路由和顶部“规则”入口；支持四类筛选、标题/别名/正文全文搜索、URL `topic` 深链、主题索引和相关主题跳转。无效主题参数回退到牌局流程，不影响账号或活动牌局。
- 上下文帮助：牌局操作区“规则”按钮定位行动与下注；GTO 弹窗中的“底池赔率”和“SPR”定位对应概念；复盘中的“摊牌”和权益/底池赔率标题提供术语入口。上下文入口先打开二级弹窗，关闭后保留牌局或复盘位置，也可继续进入完整教学中心。
- 规则边界：底池赔率明确为跟注额除以跟注后总底池；权益与 GTO 标明为未知牌本地近似，不读取 AI 隐藏牌；全下、边池、不足额加注、最佳五张牌和平局余数分配与当前引擎口径一致。
- 响应式：桌面使用左侧主题索引与右侧正文，小窗口改为横向主题索引，竖屏使用单列正文和分类下拉；顶部六个导航入口在 `360×640` 下缩小为纯图标。`1366×768`、`1024×768`、`640×520`、`360×640` 截图均无横向溢出、文字遮挡或控件重叠。
- 自动验证：`vue-tsc` 通过；12 个测试文件共 71 项单测通过，其中 3 项验证必需主题、分类、全文搜索、无效深链和全部相关链接；Vite 生产构建、Stylelint、变更文件 Prettier 和 ESLint 0 错误通过。Playwright 12 条流程全部通过，覆盖规则页深链与搜索、操作区/GTO/复盘弹窗、四档视口、复盘位置保持及查看规则前后活动牌局存档一致。

#### V-018：F-004 本地规则教学中心体验验证（已验证通过）

- 独立页面：从顶部“规则”进入教学中心，检查四类筛选、搜索、主题列表、正文和相关主题跳转；直接打开 `/#/rules?topic=pot-odds` 应定位“底池赔率”。
- 规则内容：重点核对全下与边池、不足额加注是否重新开放行动、底池赔率公式、有效筹码与 SPR、最佳五张牌、平局和余数筹码的说明是否清楚且符合预期。
- 牌局入口：在用户行动回合点击操作区“规则”，确认打开“行动与下注轮”；关闭后牌面、行动序号、筹码和可用操作不变。
- GTO 入口：打开 GTO 参考，分别点击“底池赔率”和“SPR”，确认二级弹窗定位正确；点击“在教学中心查看”后正文与所选术语一致，返回牌局仍恢复原现场。
- 复盘入口：打开一手历史记录，从“摊牌”或“权益与底池赔率”旁的帮助按钮查看术语；关闭弹窗后复盘抽屉、回放步骤和已保存模拟保持原位。
- 响应式：检查 `1366×768`、`1024×768`、`640×520`、`360×640`，确认顶部导航、分类控件、横向主题索引、正文和二级弹窗无重叠、截断或横向页面滚动。

#### 当前暂停点

| 编号  | 时间       | 暂停位置               | 暂停原因                                                                         | 继续条件                                                   |
| ----- | ---------- | ---------------------- | -------------------------------------------------------------------------------- | ---------------------------------------------------------- |
| P-018 | 2026-09-04 | DEV-027 自动验证完成后 | 规则文字的易懂程度、上下文帮助辨识度、弹窗层级和窄屏长文阅读手感仍需用户实际判断 | 已恢复：用户确认 V-018 通过，进入 F-008 牌局预设与显示密度 |

#### DEV-028：牌面边界与 F-008 牌局预设、显示密度

- 状态：已完成并经用户验证通过。
- 牌面边界修复：统一提高 `el-segmented` 悬浮态文字/背景对比度；高对比度模式为 AI 隐牌恢复明确牌背和空牌位样式，避免通用白底覆盖；AI 未亮牌时不渲染点数、花色或牌型。
- 牌局预设：新增标准、快速练习、深筹码、复盘四个内置预设；账号可保存最多 12 个自定义预设，支持新建、重命名、复制和删除。预设只保存开桌参数与体验设置，不保存 AI 实际难度、牌堆或当前牌局现场；内容随账号设置自动保存并纳入现有备份导入导出。
- 显示密度：新增标准、紧凑、竖屏三档。只切换应用外壳和牌局表现层 CSS，不改变规则引擎、随机牌堆、AI 难度或活动牌局快照；竖屏将牌局改为单列并隐藏侧栏，保留操作区可达。
- 兼容性：旧设置和旧备份缺少新增字段时归一化为标准预设/标准密度；异常自定义预设会去重、限长、限量并忽略非法项。
- 自动验证：`vue-tsc`、74 项单测、Vite 生产构建、Stylelint、ESLint、变更文件 Prettier 全部通过；Playwright 13 条流程全部通过，覆盖 segmented 悬浮对比度、高对比度真人牌可读性、AI 隐牌无信息泄漏、预设完整生命周期、账号刷新恢复、活动牌局快照不变及 `1366×768`、`1024×768`、`640×520`、`360×640` 四档无横向溢出。

#### V-018：F-004 本地规则教学中心体验验证（已验证通过）

- 用户于 2026-09-04 确认规则中心、上下文帮助、GTO/复盘入口、窄屏阅读和弹窗层级均符合预期。
- 补充检查：segmented 悬浮态已改为白字深绿底；高对比度牌面下真人点数/花色清晰，AI 未亮牌保持绿色牌背；自动检查确认 AI 隐牌没有点数、花色节点，且未显示 AI 牌型。

#### V-019：F-008 牌局预设与显示密度体验验证（已验证通过）

- 预设入口：首页“牌局预设”可选择标准、快速练习、深筹码、复盘；“管理预设”中保存当前配置、重命名、复制和删除自定义预设，确认删除不会改写已应用设置或活动牌局。
- 参数应用：选择快速练习后确认 AI 数量、买入、盲注、动画/思考时间、音效、回放速度、新手提示和显示密度同步变化；可用筹码不足时买入自动降到可用范围且有提示。
- 账号隔离：切换账号或刷新后确认各账号的自定义预设与显示密度分别保留；导出/导入账号备份后新增账号可恢复这些设置。
- 活动现场：牌局进行中切换到游戏设置，将密度改为竖屏，再返回牌局；确认牌堆、公共牌、行动序号、玩家筹码、当前行动和 AI 隐藏信息完全不变。
- 响应式：在 `1366×768`、`1024×768`、`640×520`、`360×640` 检查标准/紧凑/竖屏布局，无横向页面滚动、文字遮挡或操作区不可达。

#### 当前暂停点

| 编号  | 时间       | 暂停位置               | 暂停原因                                                                               | 继续条件                                                 |
| ----- | ---------- | ---------------------- | -------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| P-019 | 2026-09-04 | DEV-028 自动验证完成后 | 预设管理的实际操作手感、参数应用边界、账号隔离和三档显示密度需要用户在真实浏览器中确认 | 已恢复：用户确认 V-019 通过，继续 F-009 快捷键与误触保护 |

#### DEV-029：F-009 操作效率与误触保护

- 状态：开发与自动验证已完成，等待 V-020 人工验证。
- 实现：新增账号级全下、大额下注、离桌确认开关，默认开启并兼容旧设置；牌桌支持 F/C/B/A/Space/G/Esc 快捷键，输入框、下拉、弹层聚焦时暂停；触屏高风险操作需约 550ms 长按；命令携带 ID、session ID、actionSeq，重复或过期命令不会重复推进，继续沿用每次决策后的自动保存。
- 快捷键回归修复：V-020 首次验证发现快捷键均被拦截。根因是 Element Plus 会在 DOM 中保留隐藏的 popper/overlay，旧逻辑仅判断节点存在便视为弹层打开；现改为只拦截具有实际可见尺寸的交互层，Esc 优先关闭 GTO/规则弹窗，并将 B 修正为根据当前合法动作自动选择下注或加注。
- 自动验证：`vue-tsc`、74 项 Vitest、Vite 构建、Stylelint、ESLint、14 条 Playwright 全部通过；新增流程实际验证 G、Esc、输入框内 C 不触发以及焦点离开后 C 正常提交。

#### V-020：F-009 操作效率与误触保护体验验证（已验证通过）

- 请在 `1366x768` 牌桌用鼠标和键盘走完 F/C/B/A/Space/G/Esc；在 `360x640` 触屏短按高风险按钮不提交，长按约 550ms 后弹出带筹码影响的确认框；连续双击普通操作只推进一次。
- 请关闭三项确认开关分别验证直接提交/离桌，并确认输入框、下拉、弹层打开时快捷键不会误触；刷新或关闭页面后现场仍可恢复。

#### 当前暂停点

| 编号  | 时间       | 暂停位置               | 暂停原因                                                               | 继续条件                                             |
| ----- | ---------- | ---------------------- | ---------------------------------------------------------------------- | ---------------------------------------------------- |
| P-020 | 2026-09-04 | DEV-029 自动验证完成后 | 键盘映射、触屏长按阈值、确认文案和真实设备上的误触边界需要用户手动判断 | 已恢复：用户确认 V-020 通过，进入 F-010 PWA 离线安装 |

#### DEV-030：F-010 PWA 离线安装

- 状态：开发与自动验证已完成，等待 V-021 人工验证。
- 实现：新增 `manifest.webmanifest`、应用图标和原生 `sw.js`；首次访问缓存应用壳和运行时静态资源，导航请求采用网络优先并在断网时回退到本地 `index.html`，旧缓存版本在激活时清理。注册 Service Worker 后检测新版本，并在应用内提供立即更新入口；支持浏览器触发的安装提示，未触发时不影响普通网页使用。
- 数据边界：Service Worker 只缓存前端应用资源，不缓存或改写 IndexedDB 牌局、账号、历史记录、备份和设置数据；无云同步和网络业务依赖。
- 自动验证：`vue-tsc`、Vite 构建、Stylelint、ESLint、74 项 Vitest 通过；新增 Playwright 检查 manifest、Service Worker 注册、在线二次加载后断网刷新仍能恢复应用。

#### V-021：F-010 PWA 离线安装体验验证（待验证）

- 请使用支持 PWA 的浏览器打开 `http://127.0.0.1:5173`，确认浏览器提供安装入口；安装后以独立窗口打开，顶部主题色、应用名称和图标正确。
- 在线进入一局并完成一次操作，刷新确认现场保存；随后断网刷新，确认应用和当前牌局仍可打开，恢复网络后牌局自动保存仍正常。
- 当浏览器检测到新版本时，确认页面出现“立即更新”，点击后页面刷新到新版本，且账号、设置和牌局数据不丢失。

#### 当前暂停点

| 编号  | 时间       | 暂停位置               | 暂停原因                                                                 | 继续条件                                             |
| ----- | ---------- | ---------------------- | ------------------------------------------------------------------------ | ---------------------------------------------------- |
| P-021 | 2026-09-04 | DEV-030 自动验证完成后 | 安装入口、独立窗口表现、断网恢复和更新提示需要用户在真实浏览器环境中确认 | 已恢复：用户确认 V-021 通过，进入 F-011 公开数据导出 |

#### DEV-031：F-011 公开数据导出

- 状态：开发与人工验证已完成。
- 实现：新增 `src/domain/export.ts`，提供统计 CSV、单手 Markdown 和纯文本行动记录三种分享格式；统计页导出当前筛选结果，历史详情导出当前单手。CSV 使用 UTF-8 BOM 兼容表格软件，字段包含日期、牌局、位置、起手牌、等级、结果、净盈利、摊牌、VPIP、PFR、收藏和备注。
- 信息边界：Markdown 仅导出用户底牌、已亮牌且未弃牌对手底牌和公开公共牌；未亮牌 AI 底牌统一显示“未公开”，不读取或导出隐藏信息。JSON 备份仍是唯一可恢复格式。
- 自动验证：新增 2 项导出单测和 1 条导出 E2E；完整检查通过。E2E 验证 Markdown、TXT、CSV 文件扩展名、导出入口和 `360x640` 无横向溢出。

#### V-022：F-011 公开数据导出体验验证（已验证通过）

- 完成一手牌后进入“对局记录”，打开手牌详情，分别导出 Markdown 和行动文本；用文本编辑器检查文件可读，确认公共牌、用户底牌、行动顺序、结果和备注存在，未亮牌 AI 底牌显示“未公开”。
- 进入“牌局统计”，设置任意筛选条件后导出 CSV；用表格软件打开，确认中文无乱码，行数与当前筛选样本一致，列值与页面统计一致。
- 在 `1366x768`、`1024x768`、`640x520`、`360x640` 检查导出入口可达，无横向溢出；导出不改变当前牌局、账号、设置或自动保存现场。

- 验证结论：用户确认 V-022 通过，导出编码、字段、隐藏信息边界、真实下载行为和四档视口均符合预期。

#### 当前暂停点

| 编号  | 时间       | 暂停位置               | 暂停原因                                                                     | 继续条件                                       |
| ----- | ---------- | ---------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------- |
| P-022 | 2026-09-04 | DEV-031 自动验证完成后 | 导出文件的中文编码、字段可读性、隐藏信息边界和真实浏览器下载行为需要用户确认 | 已恢复：用户确认 V-022 通过，进入 F-013 AI 公平性诊断 |

#### V-020 STATUS UPDATE (2026-09-04)

- User confirmed V-020 passed after the shortcut interception fix.
- P-020 is restored and development proceeds with F-010 PWA offline installation.

#### DEV-032：F-013 AI 公平性诊断

- 状态：开发与自动验证已完成，并经用户验证通过。
- 实现：新增只读 `/diagnostics` 诊断页，并从设置页“开发工具”进入；按当前账号等级展示匹配范围、总难度范围和固定种子抽样分布；从当前账号历史记录聚合 AI 行动比例、近似响应时间、AI 离场率、玩家长期净盈利和胜率。
- 数据边界：固定种子模拟使用 120 轮、每轮 5 个候选；页面只显示聚合分布，不显示当前牌桌 AI 实际难度；诊断不写入 IndexedDB，不修改经验、等级、筹码、正式牌局或历史记录；无历史记录时显示空状态，不伪造行为结果。
- 自动验证：新增 `tests/diagnostics.test.ts`；`vue-tsc`、78 项 Vitest、Vite 构建、Stylelint 通过；新增诊断入口、聚合分布、隐藏当前难度和 360x640 无横向溢出 E2E，完整 17 条 Playwright 通过。整仓 ESLint 仍保留既有 `public/sw.js` 的 Service Worker 全局变量报错，新增文件专项 ESLint 无错误。

#### V-023：F-013 AI 公平性诊断体验验证（已验证通过）

- 在设置页进入“开发工具 > 开发诊断”，确认页面为只读聚合视图，且不显示当前牌桌 AI 的实际难度或 `ai-*` 档位信息。
- 确认当前账号等级、当前匹配范围、总范围 `1 - 12` 以及固定种子抽样分布合理；各档数量合计为 600，低等级仍能看到范围内的较高档位抽样。
- 有历史记录时确认 AI 行动比例、平均响应时间（标记为近似值）、AI 离场率、长期净盈利和玩家胜率可读；切换到无历史记录账号时确认显示“暂无足够本地记录”，不伪造结果。
- 在 `1366x768`、`1024x768`、`640x520`、`360x640` 检查标题、分布条、行为列表和指标无重叠、无横向溢出；刷新和切换账号后数据按账号隔离。
- 离开诊断页返回牌桌，确认经验、等级、筹码、正式牌局、历史记录和自动保存现场均未发生变化。

- 验证结论：用户确认 V-023 通过，诊断数据边界、聚合结果、账号隔离和四档视口表现符合预期。

#### 当前暂停点

| 编号 | 时间 | 暂停位置 | 暂停原因 | 继续条件 |
| --- | --- | --- | --- | --- |
| P-023 | 2026-09-04 | DEV-032 自动验证完成后 | 诊断数据边界、聚合结果可读性、账号隔离和四档视口表现需要用户在真实浏览器中确认 | 已恢复：用户确认 V-023 通过，进入 F-014 无障碍增强 |

#### DEV-033：F-014 无障碍增强

- 状态：开发与自动验证已完成，等待 V-024 人工验证。
- 实现：应用壳新增“跳转到主要内容”链接和可聚焦主内容区域；统一增强键盘 `:focus-visible` 可见焦点；牌面声明图像语义并保留隐藏牌信息边界；牌桌玩家列表补充座位、筹码、行动状态和可见牌型的语义播报；行动按钮补充 `aria-label` 与 `aria-keyshortcuts`；等待行动和结算结果使用 polite 状态播报；规则帮助弹窗补充无障碍名称。
- 复用现有能力：动画速度、行动音效、背景音乐、新手提示和高对比度牌面继续使用账号级设置分别控制；未新增网络依赖或持久化字段。
- 自动验证：`vue-tsc`、78 项 Vitest、Vite 构建、Stylelint 通过；新增 Playwright 流程验证跳过导航、焦点落点、玩家 `aria-label`、动作快捷键语义，完整 18 条 Playwright 通过。

#### V-024：F-014 无障碍增强体验验证（待验证）

- 使用键盘从首页开始，按 `Tab` 确认首个可操作焦点为“跳转到主要内容”；按 `Enter` 后焦点落到主内容区域，再继续 `Tab` 检查导航、账号管理、牌桌工具和操作按钮顺序稳定，无焦点陷阱。
- 进入牌桌后使用屏幕阅读器或浏览器辅助功能检查：玩家行能读出名称、座位、筹码和“行动中/等待行动/已弃牌/获胜”；公共牌、用户底牌和隐藏 AI 底牌能读出正确的公开状态；未亮牌 AI 底牌不得被播报为真实牌面。
- 确认行动按钮包含对应快捷键语义，GTO、规则帮助、账号管理等二级弹窗打开后焦点进入弹窗，关闭后回到原入口；结算和 AI 行动状态会被适度播报。
- 在标准与高对比度牌面、开启与关闭减少动画/音效/背景音乐/新手提示的组合下，确认牌面、花色、点数、状态和输赢不只依赖颜色，且没有信息泄漏。
- 在 `1366x768`、`1024x768`、`640x520`、`360x640` 检查焦点指示器、文本和操作区域无重叠、无横向溢出；刷新、切换账号和正常操作不改变自动保存现场。

#### 当前暂停点

| 编号 | 时间 | 暂停位置 | 暂停原因 | 继续条件 |
| --- | --- | --- | --- | --- |
| P-024 | 2026-09-04 | DEV-033 自动验证完成后 | 键盘焦点顺序、屏幕阅读器播报、色弱/高对比度可读性和减少动画设置需要用户在真实浏览器中确认 | 用户完成 V-024；通过后继续 F-015 本地阶段与里程碑 |
